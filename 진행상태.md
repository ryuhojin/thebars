# 구현 진행 상태

## 사용 규칙

- Codex는 한 번에 한 업무만 `[~]`로 둔다.
- `[x]`는 해당 업무의 완료 정의를 전부 통과한 경우만 사용한다.
- 모든 프론트 업무는 compact·medium·wide 검증을 포함한다.
- 사람이 검토하기 전 다음 업무를 시작하지 않는다.

## 현재 업무

```text
현재 업무: D24 — 파일럿·운영 배포
상태: 완료
최근 사후 보강: 관리자 route 이동 인증·셸 데이터 중복 호출 완화 운영 배포 — 완료
```

## 마스터 체크리스트

- [x] D00 프로젝트 기반과 반응형 앱 셸
- [x] D01 최초 관리자·로그인·로그아웃·비밀번호 변경
- [x] D02 대시보드
- [x] D03 바 등록·목록·상세
- [x] D04 사용자 계정 관리
- [x] D05 바별 회원·역할·권한
- [x] D06 바 기본 정보·영업시간·링크·통화
- [x] D07 품목 유형·고정 템플릿·포도 품종
- [x] D08 배지·색상
- [x] D09 카테고리 관리
- [x] D10 메뉴 기본 CRUD
- [x] D11 가격·상세 템플릿·내부 메모
- [x] D12 메뉴 목록·검색·일괄 수정·배지
- [x] D13 Public DTO·JSON Schema·미리보기
- [x] D14 고객 메뉴판 반응형 UI
- [x] D15 GitHub 발행
- [x] D16 Cloudflare 배포 상태·발행 이력
- [x] D17 과거 재발행·바 수명주기
- [x] D18 주문 탭 생성·목록·상세
- [x] D19 메뉴 주문 라인·수량·void
- [x] D20 기타 주문 항목·금액 조정
- [x] D21 계산 요청·정산·취소·일별 요약
- [x] D22 감사 로그·보관·운영 도구
- [x] D23 보안·접근성·성능 강화
- [x] D24 파일럿·운영 배포

## 사후 보강 기록

### 2026-06-24 14:11 KST — 관리자 route 이동 인증·셸 데이터 중복 호출 완화
상태: 완료
범위: 보호 route 이동 시 `로그인 상태 확인 중` 전체 화면이 반복 노출되고 `/api/auth/session`, `/api/dashboard`, `/current-permissions`가 중복 호출될 수 있는 프론트 라우터·앱 셸 데이터 흐름 보강
완료 항목: `ProtectedRoute`가 이미 인증 완료 상태인 내부 route 이동에서 전체 checking 화면으로 되돌아가지 않도록 변경, `/api/auth/session` 응답 60초 캐시와 in-flight dedupe 추가, 로그인·로그아웃·비밀번호 변경·세션 오류 시 auth cache 즉시 무효화, `/api/dashboard` 5초 cache/in-flight dedupe 추가, `/api/bars/:barId/current-permissions` 30초 cache/in-flight dedupe 추가, auth cache clear 이벤트로 dashboard/current-permission cache도 함께 무효화, 늦게 도착한 이전 요청이 cache를 되살리지 않도록 cache version guard 추가, compact·medium·wide E2E에서 `/dashboard` 로그인 후 `/bars` route 이동 시 `로그인 상태 확인 중` 미노출 및 session/dashboard/current-permission 추가 요청 0건 검증
검증 명령 및 결과: `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/auth-flow.spec.ts` 통과(9 tests), `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 19 files/147 tests, customer 3 files/13 tests), `npm run build` 통과(admin/customer)
반응형 검증(390/768/1440): auth-flow E2E에서 compact 390, medium 768, wide 1440 모두 로그인 후 보호 route 이동 시 같은 URL 체계와 앱 셸을 유지하고 불필요한 auth/dashboard/current-permission 호출이 발생하지 않음을 확인했다.
DB migration/API 변화: 없음. Cloudflare Functions API 호출 횟수를 줄이기 위한 프론트 cache/dedupe 변경이며 서버 계약은 유지한다.
환경 변화: GitHub `origin/main`에 commit `2f2bd97` push 완료. Cloudflare Pages `thebars-admin` production 배포 완료, 배포 URL `https://2de6f160.thebars-admin.pages.dev`, production alias `https://thebars-admin.pages.dev/login` 200 OK 확인.
남은 문제: 바 등록 자체의 mutation latency는 별도 성능 계측 대상이다. 이번 보강은 route 이동과 shell 데이터 중복 호출 완화까지만 수행했다.
완료 시각: 2026-06-24 14:11 KST

### 2026-06-24 13:15 KST — production-only D1 운영 설정과 로그인 화면 상용화 보강
상태: 완료
범위: preview D1을 운영 필수 경로에서 제외한 production-only 문서/예시 정리, production migration 절차 명확화, 로그인 화면 좌측 히어로 상용 제품 톤 개선, 바가 없는 system-admin의 품목 유형 추가 권한 회귀 수정
완료 항목: Cloudflare Pages `thebars-admin` production Functions binding `DB` 기준으로 README/runbook 정리, `admin-menu-manager/wrangler.example.toml` production D1 예시만 유지, `admin-menu-manager/.env.example`에서 D1 name/id env 예시 제거, 현재 runtime 필수 secret을 `SETUP_TOKEN`/`ADMIN_RECOVERY_TOKEN`으로 분리, 파일럿 준비 panel의 환경 gate를 `운영 D1 바인딩과 migration 확인`으로 변경, 로그인 화면 좌측의 개발 보안 용어와 단순 그라데이션 제거, THE BAR 관리자 콘솔 미니 화면·운영 지표·권한/주문/발행 중심 문구 추가, auth E2E에 개발 문구 미노출과 좌측 히어로 회귀 추가, `ItemTypesResponse.isSystemAdmin` 계약 추가, 품목 유형 화면 권한 판정을 accessibleBars 유무가 아닌 system-admin flag 기준으로 변경, no-bar system-admin 품목 유형 추가 E2E 추가, local ignored `admin-menu-manager/wrangler.toml` production-only CLI 설정 준비
검증 명령 및 결과: `npm run verify:migrations` 통과(20 migrations 빈 SQLite 적용), `npm run lint` 통과, `npm run verify:pilot` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/itemTypesApi.test.ts` 통과(5 tests), `npm run test --workspace admin-menu-manager -- tests/integration/pilotReadinessApi.test.ts` 통과(3 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과(49 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/item-types.spec.ts` 통과(6 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/auth-flow.spec.ts` 통과(6 tests), `npm run test` 통과(admin 19 files/147 tests, customer 3 files/13 tests), `npm run build` 통과(admin/customer), `npm run verify:security` 통과, `curl -I http://127.0.0.1:5173/login` 200 OK, `npx wrangler d1 migrations list <production-d1-name> --remote` 통과(적용 대기 migration 없음), `npx wrangler d1 execute <production-d1-name> --remote --command "SELECT COUNT(*) AS applied_migrations FROM d1_migrations; ..."` 결과 `applied_migrations=20` 및 운영 table 생성 확인
반응형 검증(390/768/1440): auth E2E에서 `/login`, `/setup`, `/recovery`, `/change-password` 흐름을 compact 390, medium 768, wide 1440 기준으로 확인했다. 품목 유형 no-bar E2E에서 바가 없어도 admin1이 `/system/item-types`에서 `품목 유형 추가`를 누르면 `새 공통 유형` 폼으로 전환되고 입력값이 유지됨을 390/768/1440에서 확인했다. screenshot 생성 완료 — `admin-menu-manager/test-results/auth-flow-D01-auth-screens-a2e2e-d-preserve-input-at-compact-chromium/auth-login-compact.png`, `admin-menu-manager/test-results/auth-flow-D01-auth-screens-84faa-nd-preserve-input-at-medium-chromium/auth-login-medium.png`, `admin-menu-manager/test-results/auth-flow-D01-auth-screens-render-and-preserve-input-at-wide-chromium/auth-login-wide.png`, `admin-menu-manager/test-results/item-types-D07-system-admi-6c10e--accessible-bars-at-compact-chromium/item-type-no-bar-create-compact.png`, `admin-menu-manager/test-results/item-types-D07-system-admi-154e8-t-accessible-bars-at-medium-chromium/item-type-no-bar-create-medium.png`, `admin-menu-manager/test-results/item-types-D07-system-admi-c6a16-out-accessible-bars-at-wide-chromium/item-type-no-bar-create-wide.png`
DB migration/API 변화: 새 migration 없음. production 원격 DB에는 기존 0000~0019 migration 적용 완료를 확인했다. API 계약은 `ItemTypesResponse.isSystemAdmin` boolean을 추가했다.
환경 변화: preview D1은 현재 운영 필수 경로에서 제외한다. production D1은 Cloudflare Pages binding `DB`로 연결되어야 한다.
남은 문제: production DB migration은 사람 승인 후 별도 실행한다. 실제 GitHub/Cloudflare 발행 adapter는 아직 fake adapter 경로라 publication secret은 설정하지 않는다.
완료 시각: 2026-06-24 13:24 KST

### 2026-06-24 12:46 KST — Cloudflare Pages 앱 build 수정
상태: 완료
범위: `admin-menu-manager`와 `customer-menu-board`를 Cloudflare Pages root directory로 각각 빌드할 때 production build가 Playwright 테스트 의존성에 묶이지 않도록 분리
완료 항목: `admin-menu-manager/tsconfig.build.json` 추가, `customer-menu-board/tsconfig.build.json` 추가, admin/customer build script를 production build 전용 tsconfig로 변경, Cloudflare 유사 조건의 독립 admin/customer 설치·빌드 검증, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run build --workspace admin-menu-manager` 통과. `npm run build --workspace customer-menu-board` 통과. 임시 복사본에서 `admin-menu-manager`만 `npm install --progress=false && npm run build` 실행해 `@playwright/test` 없이 통과. 임시 복사본에서 `customer-menu-board`만 `npm install --progress=false && npm run build` 실행해 `@playwright/test` 없이 통과. `npm run lint` 통과. `npm run typecheck` 통과(admin/customer). `npm run test` 통과(admin 19 files/146 tests, customer 3 files/13 tests). `npm run build` 통과(admin/customer). `npm audit --omit=dev` 통과(0 vulnerabilities). `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/auth-flow.spec.ts tests/e2e/dashboard.spec.ts tests/e2e/d23-quality.spec.ts` 통과(18 tests). `npm run test:e2e --workspace customer-menu-board` 통과(5 tests)
반응형 검증(390/768/1440): admin E2E에서 로그인·대시보드·주요 route matrix를 compact, medium, wide에서 확인했다. customer E2E에서 `/{encodedSlug}` 단일 URL의 compact, medium, wide 고객 메뉴판과 오류/schema 상태, resize fetch 안정성을 확인했다.
DB migration/API 변화: 없음. Pages production build 설정만 변경했다.
환경 변화: Cloudflare Pages 관리자 설정은 GitHub repository `ryuhojin/thebars`, root directory `admin-menu-manager`, build command `npm run build`, output directory `dist` 기준이다. 고객 앱 설정은 GitHub repository `ryuhojin/thebars`, root directory `customer-menu-board`, build command `npm run build`, output directory `dist` 기준이다.
남은 문제: 없음. Cloudflare Pages deploy는 사용자가 다시 실행해야 한다.

### 2026-06-24 12:48 KST — preview D1 원격 migration 적용
상태: 완료
범위: Cloudflare D1 preview/production 원격 DB 분리 확인, preview DB에 0000~0019 migration 적용, production DB 미적용 상태 보존
완료 항목: preview/production D1 생성 확인, local 전용 untracked `admin-menu-manager/wrangler.toml` binding 확인, preview 원격 DB에 20개 migration 적용, production에는 승인 전 migration을 적용하지 않음
검증 명령 및 결과: `npx wrangler d1 execute <preview-d1-name> --remote --command "SELECT COUNT(*) AS applied_migrations FROM d1_migrations; SELECT id, name FROM d1_migrations ORDER BY id;"` 결과 `applied_migrations=20` 및 `0000_d00_foundation.sql`~`0019_d23_rate_limits.sql` 기록 확인. `npx wrangler d1 execute <preview-d1-name> --remote --command "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;"` 결과 `bars`, `users`, `sessions`, `menu_items`, `order_tabs`, `publications`, `audit_logs`, `rate_limit_buckets`, `d1_migrations` 등 운영 schema table 생성 확인. `npx wrangler d1 execute <production-d1-name> --remote --command "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;"` 결과 Cloudflare 내부 `_cf_KV` 외 업무 table 없음 확인
DB migration/API 변화: 새 migration 파일 없음. 기존 `admin-menu-manager/db/migrations/0000_d00_foundation.sql`~`0019_d23_rate_limits.sql`을 preview 원격 DB에 적용했다. production 원격 DB에는 적용하지 않았다.
환경 변화: preview D1 `<preview-d1-name>` id `<preview-d1-id>`, production D1 `<production-d1-name>` id `<production-d1-id>`. production secret, production deploy, production migration 없음.
남은 문제: production DB migration은 backup/export, release note, 사람 승인 후 별도 실행해야 한다. 실제 Pages project secret 설정과 customer JSON 실발행 adapter 연결은 아직 운영 gate로 남아 있다.

### 2026-06-24 11:56 KST — 관리자 내비게이션·복구 CTA 보강
상태: 완료
범위: 기능별 sidebar 그룹화 후 현장 핵심 업무인 주문 운영을 compact·medium에서 최우선으로 접근 가능하게 조정, 로그인 화면의 관리자 복구 CTA 제거
참조 와이어프레임 ID 및 URL: WF-01~WF-05 인증·관리자 셸, WF-19~WF-20 주문 운영, 참고 `/dashboard`, `/login`, `/recovery`, `/bars/{barId}/orders`
완료 항목: sidebar를 주문 운영·고객 메뉴판·메뉴 관리·바 운영·시스템 관리·운영 홈 그룹으로 정리, 권한별 그룹 노출 유지, staff compact drawer와 medium sidebar에서 주문 운영이 첫 그룹으로 표시되도록 DOM 순서 조정, `/recovery` route와 API는 유지하되 로그인 화면의 `관리자 복구` 버튼 제거
검증 명령 및 결과: `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d23-quality.spec.ts tests/e2e/preview.spec.ts tests/e2e/auth-flow.spec.ts` 통과(17 tests)
반응형 검증(390/768/1440): D23 route matrix와 preview/auth E2E에서 compact 390, medium 768, wide 1440을 검증했다. staff 계정은 768 sidebar와 390 drawer 모두에서 `주문 운영` 그룹과 `주문 탭` 링크가 먼저 표시된다.
resize 상태 보존: D23 resize E2E에서 현재 작업 바, 카테고리 검색, URL, 권한 fetch 안정성이 유지됨을 확인했다.
DB migration/API 변화: 없음. `/recovery`와 `/api/recovery`는 요구사항대로 유지한다. 로그인 화면에서만 복구 CTA를 숨겼다.
남은 문제: 없음. 다음 D 업무를 시작하지 않았다.

### 2026-06-24 11:47 KST — 관리자 UI/UX 상용화 보강
상태: 완료
범위: 관리자 시스템의 리스트/그리드 표면 통일, 상용 서비스에 부적절한 개발 문구 제거, THE BAR 브랜딩 정리, 로그인 전 보호 강화, 헤더 현재 작업 바 selector와 권한별 sidebar 재검증, 품목 유형 추가 CTA 포커스 개선, local full fixture 안정화
참조 와이어프레임 ID 및 URL: WF-01~WF-22 전체 관리자 화면, 특히 `/dashboard`, `/bars`, `/bars/{barId}/settings`, `/bars/{barId}/menus`, `/bars/{barId}/publications`, `/bars/{barId}/orders`, `/system/item-types`, `/system/audit`
완료 항목: `DESIGN.md` 추가, `Bar Ops`/Dxx/WF/done/raw enum/code 중심 문구 정리, 고객 메뉴판·공개 데이터·운영 배포 등 한국어 운영 용어로 교체, 로그인하지 않은 사용자가 보호 route의 앱 셸을 보지 못하도록 route guard 강화, 관리자 header의 현재 작업 바 selector와 권한별 sidebar 노출 재검증, 데이터 표/카드 공통 스타일 정리, 체크박스 시각 크기 축소와 44px hitbox 보존, 품목 유형 추가 시 편집 패널 scroll/focus, publication/audit/bar/order/status 라벨 표시 helper 정리, local full fixture seed race guard 추가
TUI Grid 검토 결과: `@toast-ui/react-grid@4.21.22`는 공식 React wrapper가 React 16/17 peer range라 현재 React latest 앱에 즉시 추가하지 않았다. 우선 repo-native `data-table`/`data-card` 패턴을 통일했고, 대량 편집 화면에서 별도 호환성 검증 후 도입 후보로 남겼다.
검증 명령 및 결과: `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과(49 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/auth-flow.spec.ts tests/e2e/dashboard.spec.ts tests/e2e/item-types.spec.ts tests/e2e/preview.spec.ts tests/e2e/publications.spec.ts tests/e2e/audit.spec.ts tests/e2e/d24-pilot-readiness.spec.ts tests/e2e/bars.spec.ts` 통과(40 tests), `npm run test` 통과(admin 19 files/146 tests, customer 3 files/13 tests), `npm run test:e2e` 통과(admin 79 tests, customer 5 tests), `npm run build` 통과(admin/customer)
반응형 검증(390/768/1440): 전체 Playwright E2E에서 관리자 shell, auth, dashboard, bar selector, 권한별 sidebar, bar settings, menu items, item types, publications, orders, audit, system users, customer preview를 compact 390, medium 768, wide 1440에서 확인했다. 관련 screenshot은 각 `admin-menu-manager/test-results/*`와 `customer-menu-board/test-results/*`에 남았다.
resize 상태 보존: responsiveShell 및 D23/D24/E2E에서 URL 유지, 현재 작업 바, 검색/필터, 선택, bulk edit draft, audit filter, publication dialog, order state가 resize 후 유지됨을 확인했다.
DB migration: 추가 없음. 기존 migration은 수정하지 않았다.
API/환경 변화: production secret, remote push, production deploy 없음. local dev/test full fixture seed를 직렬화해 화면 테스트 중 더미 데이터가 안정적으로 적용되도록 했다.
남은 문제: 아이콘 라이브러리와 TUI Grid 실제 도입은 dependency/React 호환성 검토가 필요한 별도 후속 개선 후보로 남긴다. 이번 실행에서는 다음 D 업무를 시작하지 않았다.

## 최근 완료 기록

### 2026-06-24 10:22 KST — D24
상태: 완료
예상 변경 영역: 파일럿 readiness contract/parser, preview/production 환경 secret checklist, private customer repo/Pages 연결 절차, migration backup/rollback runbook, GitHub/Cloudflare token rotation runbook, monitoring/장애 연락 절차, 2개 바 파일럿 데이터 fixture/check, preparing→publish→republish→deactivate와 주문 open→add→adjust→settle 인수 시나리오, 실제 기기 인수 matrix, 운영자 교육 문서, 파일럿 피드백/backlog 기록, 출시 gate verifier, 관리자 기존 route 내 readiness 표시 또는 문서 연결, compact·medium·wide 전체 인수 재검증
참조 와이어프레임 ID 및 URL: WF-01~WF-22 전체 화면, 특히 `/dashboard`, `/system/audit`, `/bars/{barId}`, `/bars/{barId}/publications`, `/bars/{barId}/orders`, `/bars/{barId}/orders/{orderTabId}`, `/{encodedSlug}`
시작 메모: 가장 앞선 미완료 업무 D24만 진행한다. 실제 production secret 생성, remote push, production deploy, production migration은 수행하지 않는다. D24는 사람이 파일럿 승인 전 확인할 수 있는 운영 절차, 환경 분리 검증, 파일럿 데이터와 전체 인수 증거를 repo 안에 남기는 것이 목표다. 기존 단일 URL/권한/발행/주문 불변식은 유지하고 기기별 route는 만들지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D24 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, `pilotReadiness` contract 추가, system-admin 전용 `/api/system/pilot-readiness` API 추가, 실제 repository 상태 기반 active bar 2개·owner/manager/staff 권한 coverage·대표 메뉴 template coverage·주문 open/checkout/closed scenario readiness 계산 구현, production secret/remote push/production deploy/production migration은 manual required gate로 유지, `/system/audit` 기존 route에 파일럿 준비 panel 추가, full fixture에 `Sample Bar`와 `Whisky Lab` 2개 바·role membership·wine/whisky/cocktail/food/cigar 대표 메뉴·주문 시나리오 추가, `docs/operations/pilot-runbook.md` 추가, `docs/operations/operator-training.md` 추가, `docs/operations/pilot-feedback.md` 추가, `npm run verify:pilot` 추가, D24 backend integration test 추가, D24 Playwright compact/medium/wide 및 resize E2E 추가, responsiveShell mock 보강, medium 가로 overflow를 테스트에서 발견해 pilot panel wrapping과 root overflow clipping으로 수정, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:pilot` 통과, `npm run verify:security` 통과, `npm run verify:no-device-routes` 통과, `npm run verify:migrations` 통과(20 migrations 빈 SQLite 적용), `npm audit --omit=dev` 통과(0 vulnerabilities), `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/pilotReadinessApi.test.ts` 통과(3 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d24-pilot-readiness.spec.ts` 통과(4 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과(49 tests), `npm run test` 통과(admin 19 files/146 tests, customer 3 files/13 tests), `npm run build` 통과(admin/customer), `npm run verify:performance` 통과(admin main 283.0 KiB, customer main 265.5 KiB, admin lazy chunks 14), `npm run test:e2e` 통과(admin 79 tests, customer 5 tests)
반응형 검증(390/768/1440): D24 Playwright에서 `/system/audit`의 파일럿 준비 panel을 compact 390, medium 768, wide 1440에서 같은 URL로 확인했다. screenshot 생성 완료 — `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-compact-chromium/d24-pilot-readiness-compact.png`, `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-medium-chromium/d24-pilot-readiness-medium.png`, `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-wide-chromium/d24-pilot-readiness-wide.png`. compact·medium·wide 모두 불필요한 가로 overflow 없음과 44px touch target 기준을 통과했다.
와이어프레임 대비 차이 및 근거: D24는 WF-01~WF-22 전체 업무를 파일럿 readiness로 묶어 확인하는 운영 gate로 처리했다. 새 관리자 route를 만들지 않고 WF-21 운영 도구 `/system/audit`에 파일럿 준비 panel을 추가했다. production secret, remote push, production deploy, production migration, 실제 기기 인수, 사람 production 승인은 요구사항과 안전 규칙에 따라 실행하지 않고 `manual_required` gate와 runbook 절차로 남겼다. 회색 저충실도 스타일은 공통 디자인 토큰과 접근성 기준으로 대체했다. 기기별 URL, viewport redirect, compact/wide 별도 page tree는 추가하지 않았다.
resize 상태 보존: D24 E2E에서 `/system/audit` URL이 1440→390→768 resize 후 유지됨을 확인했다. 감사 로그 검색 `Cloudflare`와 작업 필터 `publication.requested`가 resize 후 유지되고, D24 파일럿 준비 panel과 `파일럿 시작 준비` 상태도 유지됨을 확인했다.
DB migration: 추가 없음. D24는 기존 0000~0019 migration/schema를 사용해 readiness를 읽는다. 기존 적용 migration은 수정하지 않았고 20 migrations 빈 SQLite 순차 적용 검증 완료.
API/환경 변화: `/api/system/pilot-readiness` GET 추가. 이 API는 session cookie 기반 인증과 system-admin 권한을 요구하며 mutation이 아니므로 CSRF mutation invariant를 바꾸지 않는다. local dev/test full fixture에 D24 화면 테스트용 파일럿 데이터가 추가됐다. production secret, 실제 GitHub/Cloudflare/production DB 연결, remote push, production deploy 없음.
인수 시나리오 결과: full fixture에서 `admin1`이 `/system/audit` 파일럿 준비 panel을 열면 active bar `Sample Bar`/`Whisky Lab`, owner/manager/staff coverage, 대표 template `wine`/`whisky`/`cocktail`/`food`/`cigar`, 주문 open/checkout_requested/closed scenario가 automated pass로 표시된다. preview/production D1 분리, secret checklist, private customer repo/Pages, backup/rollback, token rotation, monitoring/incident, publication lifecycle sandbox rerun, 실제 기기 인수, P0/P1/security/backup/마지막 성공 고객 메뉴판/feedback/production 승인 gate는 사람이 확인해야 하는 manual gate로 표시된다.
남은 문제: 실제 production 승인, 실제 production secret 입력, remote push/production deploy, production migration, 실제 매장 기기 현장 확인은 사람과 운영 환경의 수동 gate로 남긴다. Codex는 이 금지된 작업들을 수행하지 않았고 D24 문서와 readiness API에 수동 확인으로 기록했다. 개발계획의 D00~D24는 모두 완료했으며 다음 업무를 시작하지 않았다.
완료 시각: 2026-06-24 10:41 KST
사람 검토 결과: 미검토

### 2026-06-24 09:48 KST — D23
상태: 완료
예상 변경 영역: 보안 rate limit contract/DB/repository/service/middleware, mutation CSRF·tenant IDOR·redaction·session/security header 회귀 테스트, 접근성·반응형 전체 route matrix 보강, keyboard/focus/touch/contrast/360px/200% 확인, 성능 budget·중복 fetch·route lazy loading 점검, fake external failure suite, 전체 gate 및 문서 갱신
참조 와이어프레임 ID 및 URL: WF-01~WF-22 전체 화면, 특히 `/setup`, `/login`, `/dashboard`, `/system/users`, `/system/audit`, `/bars/{barId}/menus`, `/bars/{barId}/orders`, `/bars/{barId}/orders/{orderTabId}`, `/bars/{barId}/publications`, `/{encodedSlug}`
시작 메모: 가장 앞선 미완료 업무 D23만 진행한다. 기능 추가보다 파일럿 전 운영 품질 보강이 목표다. 보안 invariant(CSRF, tenant 격리, secret/log redaction, session cookie/security headers, rate limit), 반응형/접근성 invariant(전체 주요 route matrix, resize 상태 보존, keyboard/focus/touch/contrast/360px/200%), 성능 invariant(N+1/중복 viewport fetch, pagination/점진 로딩, bundle budget/lazy loading, 고객 메뉴판 Web Vitals 기준)을 현재 코드 기준으로 검증·보강한다. production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D23 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, `rate_limit_buckets` contract/migration/schema 추가, memory/D1 rate limit repository와 service/runtime 구현, setup/recovery/login/publish/settle rate limit 적용, settle idempotency 재시도 선처리 보존, 429 `RATE_LIMITED` 오류 contract 추가, dev full fixture reset 시 rate limit bucket 초기화, mutation CSRF 정적 검증 추가, HTTPS session/security header 회귀 테스트 추가, publication/order settle/auth rate limit 통합 테스트 추가, 전체 migration verifier를 migration directory 기반으로 확장, admin route lazy loading과 performance budget verifier 추가, AdaptiveDialog focus trap/restore/Escape 구현, D23 전체 관리자 route matrix E2E 추가, keyboard-only/360px/200% zoom/focus restore E2E 추가, resize 상태 보존과 viewport 중복 fetch 방지 E2E 추가, customer Web Vitals/fetch 안정성 E2E 추가, production dependency audit 통과, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:security` 통과, `npm run verify:no-device-routes` 통과, `npm run verify:migrations` 통과(20 migrations 빈 SQLite 적용), `npm audit --omit=dev` 통과(0 vulnerabilities), `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/authApi.test.ts tests/integration/orderTabsApi.test.ts tests/integration/publicationsApi.test.ts` 통과(33 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과, `npm run test` 통과(admin 18 files/143 tests, customer 3 files/13 tests), `npm run build` 통과(admin/customer), `npm run verify:performance` 통과(admin main 283.0 KiB, customer main 265.5 KiB, admin lazy chunks 14), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d23-quality.spec.ts` 통과(6 tests), `npm run test:e2e --workspace customer-menu-board` 통과(5 tests), `npm run test:e2e` 통과(admin 75 tests, customer 5 tests)
반응형 검증(390/768/1440): D23 Playwright route matrix에서 `/dashboard`, `/bars`, `/bars/new`, `/bars/{barId}`, members/settings/categories/menus/new/menu detail/preview/publications/orders/order detail, `/system/users`, `/system/audit`, `/system/item-types`, `/system/badges`를 compact 390, medium 768, wide 1440에서 같은 URL로 확인했다. screenshot 생성 완료 — `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-compact-chromium/d23-route-matrix-compact.png`, `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-medium-chromium/d23-route-matrix-medium.png`, `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-wide-chromium/d23-route-matrix-wide.png`. 고객 메뉴판은 390→768→1440→390 resize에서 같은 `/{encodedSlug}` URL과 단일 JSON fetch를 확인했다.
와이어프레임 대비 차이 및 근거: WF-01~WF-22 전체 화면의 상태·접근성·반응형 검증 업무로 처리했다. 와이어프레임의 관리자 header 바 선택 요구는 기존 `현재 작업 바` selector와 full fixture 다중 바로 재검증했고, sidebar는 권한별 메뉴 노출만 유지했다. 회색 저충실도 표현은 복사하지 않고 기존 디자인 토큰, 44px touch target, keyboard focus 기준으로 검증했다. 기기별 URL, viewport redirect, compact/wide 별도 page tree는 추가하지 않았다.
resize 상태 보존: D23 E2E에서 manager가 `Whisky Lab`을 선택하고 category search `위스키`를 입력한 뒤 1440→390→768 resize 후 선택 바, search 값, URL이 유지됨을 확인했다. resize 중 `/api/dashboard`와 `/api/current-permissions` 중복 fetch가 발생하지 않음을 확인했다. 고객 메뉴판은 검색/JSON 요청 안정성을 390→768→1440→390에서 확인했다.
DB migration: `admin-menu-manager/db/migrations/0019_d23_rate_limits.sql` 추가. `rate_limit_buckets` table과 `(scope,key_hash)` unique index, expiry index를 추가했다. 기존 적용 migration은 수정하지 않았고 0000~0019 빈 SQLite 순차 적용 검증 완료.
API/환경 변화: `/api/setup`, `/api/recovery`, `/api/auth/login`, `/api/bars/:barId/publications`, `/api/bars/:barId/order-tabs/:tabId/settle`에 rate limit 적용. rate limit bucket key는 SHA-256 hash로 저장해 username/IP/token 원문을 저장하지 않는다. local dev full fixture reset은 rate limit bucket도 초기화한다. production secret, 실제 GitHub/Cloudflare/production DB 연결, remote push, production deploy 없음.
인수 시나리오 결과: admin full fixture에서 `admin1`, `owner1`, `manager1`, `staff1` 계정의 header `현재 작업 바` selector와 `Sample Bar`/`Whisky Lab` 선택을 확인했다. manager는 `Whisky Lab` 선택 후 카테고리·메뉴·주문에 접근 가능하고, staff는 preview/orders 등 허용 메뉴만 sidebar에 노출된다. keyboard-only login, compact sidebar open/close, system user dialog focus trap/restore, 360px/200% zoom, route별 가로 overflow 없음, 44px touch target을 E2E로 확인했다.
남은 문제: D24 파일럿·운영 배포 미착수. D23은 파일럿 전 품질 gate 강화까지만 완료했고 다음 업무를 시작하지 않았다.
완료 시각: 2026-06-24 10:20 KST
사람 검토 결과: 미검토

### 2026-06-24 09:24 KST — D22
상태: 완료
예상 변경 영역: audit/maintenance contract, `audit_logs`/`maintenance_runs` migration/schema, memory/D1 audit repository, audit/maintenance service, 중요 운영 작업 audit 기록, `/api/system/audit` list/summary와 maintenance dry-run/execute API, `/system/audit` UI, system-admin sidebar, WF-21 compact·medium·wide 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-21 `/system/audit`, 참고 WF-18 `/bars/{barId}/publications`, WF-20 `/bars/{barId}/orders/{orderTabId}`
시작 메모: 가장 앞선 미완료 업무 D22만 진행한다. 이번 업무는 중요 작업 추적, 민감정보 redaction, 시스템 관리자 전용 감사 로그 조회, 보관 대상 dry-run/maintenance 실행 기록까지다. 주문 closed/cancelled 1년, 일별 정산 요약 3년 보관 정책을 유지하고 자동 스케줄은 만들지 않는다. 화면 폭/기기 정보는 감사 로그 권한 근거로 저장하지 않는다. production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D22 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, audit/maintenance contract 추가, `0018_d22_audit_maintenance.sql` migration 추가, Drizzle schema 추가, memory/D1 audit repository 추가, order/publication retention preview·prune repository 메서드 추가, audit service와 redaction/sanitize 구현, 로그인 성공·실패 audit 기록, 주요 mutation route audit mapper 구현, `/api/system/audit` 조회 API 구현, `/api/system/audit/maintenance-runs` dry-run/execute API 구현, `/system/audit` UI 구현, system-admin sidebar 감사 로그 메뉴 추가, staff 접근 차단 화면 구현, local full fixture 감사 로그·maintenance 샘플 추가, backend 통합 테스트 추가, responsiveShell route/resize 테스트 추가, Playwright 390/768/1440 E2E와 screenshot 추가, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(18 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npx vitest run --config vitest.config.ts tests/integration/auditApi.test.ts` 통과(4 tests), `npx vitest run --config vitest.config.ts tests/e2e/responsiveShell.test.tsx` 통과(49 tests), `npm run test` 통과(admin 18 files/139 tests, customer 3 files/13 tests), `npm run build` 통과(admin/customer; admin Vite 500KB chunk warning은 non-failing), `npm run test:e2e -- audit.spec.ts` 통과(5 tests), `npm run test:e2e` 통과(admin 69 tests, customer 4 tests), local dev server `http://127.0.0.1:5173/system/audit` 200 응답 확인
반응형 검증(390/768/1440): Playwright screenshot 생성 완료 — `admin-menu-manager/test-results/audit-D22-audit-log-page-at-compact-chromium/audit-compact.png`, `admin-menu-manager/test-results/audit-D22-audit-log-page-at-medium-chromium/audit-medium.png`, `admin-menu-manager/test-results/audit-D22-audit-log-page-at-wide-chromium/audit-wide.png`. WF-21의 search/filter/table/card/detail/maintenance 구조를 같은 `/system/audit` route에서 확인했다.
와이어프레임 대비 차이 및 근거: WF-21의 actor/request 검색, operation/bar/date 필터, wide table, compact event card, safe metadata detail, 보관 작업 버튼을 구현했다. 와이어프레임에는 구체 삭제 결과 수치가 고정되어 있지 않아 D22 보관 정책 수치(주문 365일, 일별 요약 3년, 발행 성공/실패 100건)를 maintenance panel metric으로 표시했다. 회색 저충실도 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 별도 운영 앱, 기기별 URL, viewport redirect는 추가하지 않았다.
resize 상태 보존: D22 Playwright에서 `/system/audit` URL 유지, search `Cloudflare`, operation `publication.requested`, result `failure`, dry-run 완료 결과가 1440→390 resize 후 유지됨을 확인했다. Vitest responsive shell에서도 audit search, operation/result filter, 선택 이벤트, maintenance dry-run 결과가 resize 후 유지됨을 확인했다. 필터 입력 중 loading remount로 값이 끊기던 문제와 compact 긴 error code badge overflow를 테스트에서 발견해 수정했다.
DB migration: `admin-menu-manager/db/migrations/0018_d22_audit_maintenance.sql` 추가. `audit_logs` table, `maintenance_runs` table, actor/bar/operation/result/request 관련 index를 추가했다. 기존 0000~0017 migration은 수정하지 않았고 0000~0018 빈 SQLite 순차 적용 검증 완료.
API/환경 변화: `/api/system/audit` GET 추가, `/api/system/audit/maintenance-runs` POST 추가. 주요 mutation route audit mapper는 응답 후 기록하며 audit 실패가 원 업무 응답을 실패시키지 않는다. local dev/test full fixture에 audit log 4건과 maintenance dry-run 샘플을 추가했다. production secret, 실제 GitHub/Cloudflare/production DB 연결, remote push, production deploy 없음.
인수 시나리오 결과: admin full fixture에서 `/system/audit`에 진입해 발행 요청, 주문 정산, 사용자 잠금 해제, Cloudflare 확인 필요 실패 샘플을 확인하고 actor/bar/operation/result/search filter, safe metadata detail, dry-run 완료 결과를 compact·medium·wide에서 확인했다. staff full fixture는 sidebar에 감사 로그 메뉴가 없고 직접 `/system/audit` 접근 시 시스템 관리자 권한 필요 화면이 표시된다. backend 통합 테스트에서 401/403, 로그인 실패 redaction, audit 필터 조회, maintenance dry-run no-delete, execute terminal 데이터 삭제와 open 탭 보존을 확인했다.
남은 문제: D23 보안·접근성·성능 강화 미착수. D22는 감사 로그·보관·운영 도구까지만 완료했고 다음 업무를 시작하지 않았다.
완료 시각: 2026-06-24 09:45 KST
사람 검토 결과: 미검토

### 2026-06-24 09:11 KST — D13 관리자 셸 바 선택·권한 sidebar 회귀 보강
상태: 완료
예상 변경 영역: 관리자 AppShell 현재 작업 바 selector, 선택 바 변경 시 권한 state reset, 권한 응답 barId 일치 검증, 권한 응답 전 역할 기본 sidebar 계산, local full fixture 다중 바 데이터, D13 preview/sidebar E2E, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-05 `/dashboard` 관리자 셸, WF-14 `/bars/{barId}/categories`, 참고 WF-17 `/bars/{barId}/preview`
시작 메모: 사용자가 지적한 관리자 header 바 선택과 권한별 sidebar 노출 문제는 D13 완료 기능의 회귀 보강으로 처리한다. D22 감사 로그·보관 업무는 시작하지 않는다. 기기별 route, 화면 폭 redirect, production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 관리자 header selector label을 `현재 작업 바`로 명확히 유지, 선택 바 변경 시 stale permissions 즉시 reset, permissions 응답의 `barId`가 현재 선택 바와 일치할 때만 sidebar 계산에 사용, 응답 대기 중 owner/manager/staff 역할 기본 권한으로 sidebar 초기 계산, local full fixture에 `Whisky Lab` 두 번째 바·카테고리·메뉴와 owner/manager/staff 소속 추가, manager의 바 선택 후 카테고리 접근 E2E 추가, staff 제한 메뉴 hidden E2E 강화, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run lint` 통과, `npm run verify:migrations` 통과(18 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과(45 tests), `npm run test` 통과(admin 17 files/131 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/preview.spec.ts` 통과(5 tests), `npm run test:e2e` 통과(admin 64 tests, customer 4 tests), `npm run build` 통과(admin/customer; admin Vite chunk size warning은 non-failing), local dev server `http://127.0.0.1:5173/` 실행 및 full fixture reset 204 확인
반응형 검증(390/768/1440): manager 계정으로 `Whisky Lab` 선택 후 같은 `/bars/{barId}/categories` URL에서 heading, selector value, 가로 overflow 없음 확인. screenshot 저장: `/private/tmp/thebar-manager-shell-compact.png`, `/private/tmp/thebar-manager-shell-medium.png`, `/private/tmp/thebar-manager-shell-wide.png`. D13 preview E2E도 compact·medium·wide 3폭 통과.
와이어프레임 대비 차이 및 근거: WF-05 header의 bar selector 요구를 관리자 AppShell의 `현재 작업 바` selector로 명확히 유지했다. WF-14 카테고리는 manager/owner의 `can_edit_menu` 권한에서 접근 가능하고 staff는 sidebar에서 숨긴다. compact/medium/wide 모두 같은 URL과 같은 route component를 사용하며 기기별 URL이나 redirect는 추가하지 않았다.
resize 상태 보존: 실제 브라우저와 E2E에서 선택 바 값이 `/bars/{barId}/categories` 이동 후 유지되고, manager의 카테고리 접근 권한과 staff 제한 메뉴 숨김이 role별로 유지됨을 확인했다. `AppShell`은 선택 바 변경 시 이전 바 permission을 비워 다른 바 sidebar에 stale 권한이 섞이지 않도록 했다.
DB migration: 추가 없음. 기존 0000~0017 migration을 수정하지 않고 빈 SQLite 순차 적용 검증 완료.
API/환경 변화: production API 변경 없음. local dev/test full fixture에 `Whisky Lab` 두 번째 바, 빈 카테고리, `랩 올드패션드` 메뉴, owner/manager/staff 소속을 추가했다. 화면 확인용 계정은 `admin1`/`owner1`/`manager1`/`staff1`이다.
인수 시나리오 결과: 실제 브라우저에서 `admin1`, `owner1`, `manager1`, `staff1` 모두 header `현재 작업 바` selector에 `Sample Bar`와 `Whisky Lab` 두 옵션이 노출됨을 확인했다. admin은 전체 메뉴, owner/manager는 카테고리·메뉴·주문·품목·배지, staff는 미리보기·주문만 노출됨을 확인했다. manager는 `Whisky Lab` 선택 후 sidebar 카테고리 action으로 `/bars/{barId}/categories`에 접근 가능하다.
남은 문제: D22 감사 로그·보관·운영 도구 미착수. 이번 실행은 D13 관리자 셸 회귀 보강만 처리했고 다음 업무를 시작하지 않았다.
완료 시각: 2026-06-24 09:11 KST
사람 검토 결과: 미검토

### 2026-06-23 17:22 KST — D21
상태: 완료
예상 변경 영역: checkout/request/reopen/settle/cancel contract, final total snapshot fields, `daily_order_summaries` migration/schema, memory/D1 order tab state transition repository, settle idempotency, daily summary upsert/rebuild, `/api/bars/{barId}/order-tabs/{tabId}` 상태 전이 API, `/bars/{barId}/orders` 계산 요청 큐·일별 요약, `/bars/{barId}/orders/{orderTabId}` 합계·정산·취소·재오픈 UI, compact 하단 고정 총액/정산 action, WF-19/WF-20/WF-05 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-20 `/bars/{barId}/orders/{orderTabId}`, WF-19 `/bars/{barId}/orders`, 참고 WF-05 `/dashboard`
시작 메모: 가장 앞선 미완료 업무 D21만 진행한다. 이번 업무는 open→checkout_requested, checkout_requested→open, checkout_requested/open→closed 정산, empty/all-void 탭 cancel, 정산 최종 합계 고정, settle idempotency, 일별 요약 기본 화면까지다. closed/cancelled immutable, version/expectedVersion, active line 서버 합계, tenant/권한 격리, 단일 URL 반응형 규칙을 유지한다. 감사 로그·보관·운영 도구 심화는 D22 범위로 남긴다. production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D21 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-20/WF-19/WF-05 compact·medium·wide 기준 확인, checkout/reopen/settle/cancel contract 추가, `0017_d21_order_settlement.sql` migration 추가, `order_tabs` 최종 정산/취소 snapshot 컬럼 추가, `daily_order_summaries` schema 추가, `order_tab_events`와 `idempotency_keys` constraint 확장, memory/D1 repository 상태 전이와 daily summary rebuild/upsert 구현, order service의 `can_manage_orders` guard 유지, settle idempotency와 reused key 409 구현, closed/cancelled immutable과 empty settle/all-void cancel guard 구현, `/api/bars/:barId/order-tabs/:tabId/checkout-request`/`reopen`/`settle`/`cancel` POST 추가, `/bars/{barId}/orders` 오늘 정산 요약·계산 요청 큐 추가, `/bars/{barId}/orders/{orderTabId}` 정산·취소·재오픈 panel과 compact sticky 총액/정산 action 추가, local full fixture D21 데이터 추가, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(18 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts` 통과(9 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx` 통과(45 tests), `npm run test` 통과(admin 17 files/131 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts` 통과(6 viewport/permission tests), `npm run test:e2e` 통과(admin 63 tests, customer 4 tests), `npm run build` 통과(admin/customer; admin Vite chunk size warning은 non-failing)
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `admin-menu-manager/test-results/orders-D21-checkout-settle-a0afc-mmary-and-resize-at-compact-chromium/orders-compact.png`, `admin-menu-manager/test-results/orders-D21-checkout-settle-30176-ummary-and-resize-at-medium-chromium/orders-medium.png`, `admin-menu-manager/test-results/orders-D21-checkout-settle-3f3d1--summary-and-resize-at-wide-chromium/orders-wide.png`. WF-20 주문 상세의 정산 panel과 WF-19 계산 요청 큐·일별 요약 구조를 확인했다.
와이어프레임 대비 차이 및 근거: WF-20의 주문 상세 route를 유지하고 D21 범위인 계산 요청, 재오픈, 계좌이체 확인 정산, 취소, 최종 합계 표시를 같은 `/bars/{barId}/orders/{orderTabId}` 화면에 추가했다. WF-19에는 오늘 정산 요약과 계산 요청 큐를 추가했다. wide에서는 주문 라인과 정산 panel이 같은 상세 영역에서 나란히 읽히고, compact에서는 동일 state의 하단 sticky 총액·정산 action으로 접근한다. 와이어프레임의 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 감사 로그 상세·보관 운영 도구는 D22 범위라 D21에서는 구현하지 않았다.
resize 상태 보존: D21 E2E에서 `/bars/{barId}/orders/{orderTabId}` 상세 URL, 상세 손님 표시/메모 draft, 헤더 현재 작업 바 selector, 정산 메모, 계좌이체 확인 checkbox가 resize 후 유지됨을 확인. manager 계정으로 메뉴 라인, custom/adjustment, void, 계산 요청, 정산 완료, 최종 합계 고정을 compact·medium·wide 모두 확인했다. staff 계정은 compact·medium·wide 모두 기타 항목·금액 조정 action이 렌더링되지 않음을 회귀 확인했다.
DB migration: `admin-menu-manager/db/migrations/0017_d21_order_settlement.sql` 추가. `order_tabs`, `order_tab_events`, `idempotency_keys`를 check constraint 확장용으로 재작성하고 기존 row를 보존 copy한다. `daily_order_summaries` table과 `bar_id,business_date` unique index를 추가한다. 빈 SQLite에 0000~0017 순차 적용 검증 완료.
API/환경 변화: 주문 탭 상태 전이 API 4개 추가. 주문 목록 응답에 `dailySummary`, 주문 상세 응답에 `finalTotalAmountMinor`, `settledAt`, `cancelledReason` 등 D21 snapshot field를 포함한다. local dev/test full fixture에서 `B2` 계산 요청 대기, `C4` 정산 완료, `D1` 취소 샘플을 seed한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
인수 시나리오 결과: manager full fixture에서 신규 탭 생성, 메뉴·custom·adjustment 라인 추가, 할인 void 후 합계 66,000 KRW 확인, 계산 요청, 정산 메모와 계좌이체 확인 checkbox를 resize 후 보존, 정산 완료 후 final total 66,000 KRW 고정과 상세 form 비활성화를 compact·medium·wide 모두 확인했다. API 통합 테스트에서 double settle idempotency, 다른 key 재정산 차단, active 라인 남은 cancel 차단, empty/all-void cancel 허용, daily summary settled/cancelled count를 확인했다.
남은 문제: D22 감사 로그·보관·운영 도구 미착수. D21은 주문 상태 전이와 일별 요약 기본 화면까지만 완료했으며 감사 로그 상세 필터, 보관 정책 실행, 운영 도구는 다음 업무 범위다.
완료 시각: 2026-06-23 17:43 KST
사람 검토 결과: 미검토

### 2026-06-23 17:03 KST — D20
상태: 완료
예상 변경 영역: custom/adjustment order item contract, amount/reason validators, permission-specific service guards, `order_tab_items` item type/data migration, memory/D1 order item repository, order service custom/adjustment add with idempotency and totals/version guard, `/bars/{barId}/orders/{orderTabId}` 기타 항목·금액 조정 UI, compact·medium·wide WF-20 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-20 `/bars/{barId}/orders/{orderTabId}`, 참고 WF-19 `/bars/{barId}/orders`
시작 메모: 가장 앞선 미완료 업무 D20만 진행한다. 이번 업무는 메뉴에 없는 custom 항목과 음수 할인/양수 추가금 adjustment 추가, 사유 필수, `can_add_custom_order_item`/`can_apply_order_adjustment` 별도 권한, active line 합계와 void 후 합계 복원까지다. 계산 요청, 정산, 취소, 일별 요약은 D21 범위로 남긴다. 주문 추가는 idempotency key를 사용하고 mutation은 `expectedVersion`으로 stale update 409를 반환한다. closed/cancelled 탭 mutation은 차단한다. 기기별 URL, 화면 폭 redirect, production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D20 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-20/WF-19 compact·medium·wide 기준 확인, custom/adjustment contract와 사유·금액 validator 추가, 주문 상세 권한 DTO 추가, `0016_d20_order_custom_adjustments.sql` migration 추가, `order_tab_items` item type/reason/signed amount schema 확장, idempotency operation 확장, memory/D1 repository custom/adjustment add 구현, order service의 `can_add_custom_order_item`/`can_apply_order_adjustment` 별도 guard와 staff 403 구현, custom/adjustment idempotency와 reused key 409 구현, 음수 총액 422 guard 구현, adjustment quantity update 차단, `/api/bars/:barId/order-tabs/:tabId/items/custom` POST와 `/api/bars/:barId/order-tabs/:tabId/items/adjustments` POST 추가, `/bars/{barId}/orders/{orderTabId}` 기타 항목·할인/추가금 form과 예상 금액 preview 구현, staff action hidden 구현, full fixture D20 라인 seed 추가, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(17 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts` 통과(7 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/integration/orderTabsApi.test.ts` 통과(52 tests), `npm run test` 통과(admin 17 files/129 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts` 통과(6 viewport/permission tests), `npm run test:e2e` 통과(admin 63 tests, customer 4 tests), `npm run build` 통과(admin/customer; admin Vite chunk size warning은 non-failing)
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-compact-chromium/orders-compact.png`, `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-medium-chromium/orders-medium.png`, `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-wide-chromium/orders-wide.png`. WF-20 주문 상세의 주문 라인·정산 패널 구조와 WF-19 목록 진입 기준을 확인했다.
와이어프레임 대비 차이 및 근거: WF-20의 주문 상세 route를 유지하고 D20 범위인 기타 항목·할인·추가금 action을 같은 `/bars/{barId}/orders/{orderTabId}` 화면에 추가했다. wide에서는 상세 패널 안의 섹션으로, compact에서는 같은 섹션이 1열 form으로 접힌다. 와이어프레임의 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 계산 요청, 정산, 취소, 일별 요약은 D21 범위라 D20에서는 구현하지 않았다.
resize 상태 보존: D20 E2E에서 `/bars/{barId}/orders/{orderTabId}` 상세 URL, 신규 주문 탭 상세 form draft, 헤더 현재 작업 바 selector가 resize 후 유지됨을 확인. manager 계정으로 메뉴 라인, custom 커버차지, 할인 adjustment, 추가금 adjustment, 할인 void 후 합계 복원을 compact·medium·wide 모두 확인했다. staff 계정은 compact·medium·wide 모두 기타 항목·금액 조정 action이 렌더링되지 않음을 확인했다.
DB migration: `admin-menu-manager/db/migrations/0016_d20_order_custom_adjustments.sql` 추가. `order_tab_events`, `order_tab_items`, `idempotency_keys`를 check constraint 확장용으로 재작성하고 기존 D19 row를 보존 copy한다. 빈 SQLite에 0000~0016 순차 적용 검증 완료.
API/환경 변화: custom 주문 항목 추가 API와 adjustment 추가 API 2개 추가. 주문 상세 응답에 D20 권한 booleans를 포함한다. local dev/test full fixture에서 `Bar 3` 탭에 하우스 하이볼 2개, 커버차지 2개, 단골 할인 1개를 seed한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
인수 시나리오 결과: manager full fixture에서 주문 상세에 진입해 메뉴 주문을 추가·수량 변경한 뒤 커버차지 10,000 KRW를 추가하고, 단골 할인 -4,000 KRW와 잔 파손 추가금 2,000 KRW를 적용한 뒤 할인 라인을 void해 최종 합계가 66,000 KRW로 복원됨을 확인했다. staff full fixture에서는 같은 상세 URL에서 기타 항목·금액 조정 form이 노출되지 않음을 확인했다.
남은 문제: D21 계산 요청·정산·취소·일별 요약 미착수. D20은 custom/adjustment line 추가와 void 합계 복원까지만 완료했으며 checkout_requested 표시, 정산 closed 처리, 취소, 일별 요약은 의도적으로 구현하지 않았다.
완료 시각: 2026-06-23 17:20 KST
사람 검토 결과: 미검토

### 2026-06-23 16:39 KST — D19
상태: 완료
예상 변경 영역: order tab item contract/menu snapshot/idempotency, `order_tab_items`/`idempotency_keys` migration, memory/D1 order item repository, order service line add/update/void with totals/version guard, menu picker API, `/bars/{barId}/orders/{orderTabId}` 메뉴 검색·가격 선택·수량·void UI, compact·medium·wide WF-20 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-20 `/bars/{barId}/orders/{orderTabId}`, 참고 WF-19 `/bars/{barId}/orders`
시작 메모: 가장 앞선 미완료 업무 D19만 진행한다. 이번 업무는 메뉴/가격 스냅샷 라인 추가, 수량 변경, void와 서버 정수 합계까지이며 custom item, adjustment, checkout/reopen/settle/cancel, daily summary는 D20~D21 범위로 남긴다. 주문 추가는 idempotency key를 사용하고, 수량·void는 `expectedVersion`으로 stale update 409를 반환한다. closed/cancelled 탭 mutation은 차단한다. 기기별 URL, 화면 폭 redirect, production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D19 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-20/WF-19 compact·medium·wide 기준 확인, 주문 라인 contract/menu picker/idempotency schema 추가, `0015_d19_order_tab_items.sql` migration 추가, `order_tab_items`/`idempotency_keys` Drizzle schema 추가, memory/D1 order item repository와 idempotency record 구현, order service의 active membership·`can_manage_orders` guard 유지, 메뉴 가격 snapshot line add, idempotent 재시도, reused key 409, 수량 변경, void reason/event, active line total 재계산, closed/cancelled mutation 차단 구현, `/api/bars/:barId/order-tabs/:tabId/items` POST, `/api/bars/:barId/order-tabs/:tabId/items/:itemId` PATCH, `/api/bars/:barId/order-tabs/:tabId/items/:itemId/void` POST 추가, `/bars/{barId}/orders/{orderTabId}` 메뉴 검색·가격 선택·수량·void UI 구현, checkout_requested 재오픈 확인 UI 추가, full fixture 주문 라인 seed 추가, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(16 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts` 통과(5 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/integration/orderTabsApi.test.ts` 통과(50 tests), `npm run test` 통과(admin 17 files/127 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts` 통과(3 viewport tests), `npm run test:e2e` 통과(admin 60 tests, customer 4 tests), `npm run build` 통과(admin/customer; admin Vite chunk size warning은 non-failing), 로컬 서버 `http://127.0.0.1:5173/` 화면 확인 가능
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `admin-menu-manager/test-results/orders-D19-order-line-add--82f64--void-and-resize-at-compact-chromium/orders-compact.png`, `admin-menu-manager/test-results/orders-D19-order-line-add-quantity-void-and-resize-at-medium-chromium/orders-medium.png`, `admin-menu-manager/test-results/orders-D19-order-line-add-quantity-void-and-resize-at-wide-chromium/orders-wide.png`. WF-20 주문 상세의 라인/수량/void 구조와 WF-19 목록 진입 기준을 확인했다.
와이어프레임 대비 차이 및 근거: WF-20의 주문 상세 구조를 같은 `/bars/{barId}/orders/{orderTabId}` route에 유지하고, D19 범위인 메뉴 검색·가격 선택·수량·line void action을 추가했다. 와이어프레임의 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 기타 주문 항목, 금액 조정, 계산 요청, 정산, 취소, 일별 요약은 D20~D21 범위라 D19에서는 구현하지 않았다.
resize 상태 보존: D19 E2E에서 `/bars/{barId}/orders/{orderTabId}` 상세 URL, 신규 주문 탭 상세 form draft, 헤더 현재 작업 바 selector가 resize 후 유지됨을 확인. compact·medium·wide 모두 메뉴 line add, 수량 +1 변경, void reason 제출, 불필요한 가로 스크롤 없음, 44px touch target을 확인했다.
DB migration: `admin-menu-manager/db/migrations/0015_d19_order_tab_items.sql` 추가. `order_tab_items`, `idempotency_keys`, 주문 event type 확장, line total check, active/voided consistency check, bar/tab/status/idempotency index를 빈 SQLite에 순차 적용 검증 완료.
API/환경 변화: 주문 라인 추가·수량 변경·void API 3개 추가. 주문 상세 응답에 `items`와 `menuPicker`를 포함한다. local dev/test full fixture에 `A1` 맥캘란 샷 1개와 `Bar 3` 하우스 하이볼 2개를 seed한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
인수 시나리오 결과: staff full fixture에서 헤더 바 selector로 Sample Bar를 유지한 채 주문 상세에 진입하고, 신규 주문 탭을 만든 뒤 메뉴 picker에서 맥캘란 가격을 2개 추가, 서버 snapshot 합계 36,000 KRW 확인, 수량 +1 후 54,000 KRW 확인, reason `오입력`으로 void 처리 후 active 합계 0 KRW와 void reason 표시를 확인했다.
남은 문제: D20 기타 주문 항목·금액 조정 미착수. D21 계산 요청·정산·취소·일별 요약 미착수. D19는 메뉴 기반 라인만 완료했으며 custom item, adjustment, checkout/settle/cancel, daily summary는 의도적으로 구현하지 않았다.
완료 시각: 2026-06-23 17:01 KST
사람 검토 결과: 미검토

### 2026-06-23 16:13 KST — D18
상태: 완료
예상 변경 영역: order tab contract/status/version/filter/summary, `order_tab_counters`/`order_tabs`/`order_tab_events` migration, memory/D1 order repository, order service state/version guard, `/api/bars/{barId}/order-tabs` list/create, `/api/bars/{barId}/order-tabs/{tabId}` detail/update, `/bars/{barId}/orders`와 `/bars/{barId}/orders/{tabId}` UI, 권한별 sidebar 주문 메뉴, compact·medium·wide WF-19 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-19 `/bars/{barId}/orders`, 참고 WF-20 `/bars/{barId}/orders/{orderTabId}`, WF-05 `/dashboard`
시작 메모: 가장 앞선 미완료 업무 D18만 진행한다. 이번 업무는 주문 탭 생성·목록·상세와 설명 수정까지이며 메뉴 주문 라인, 수량, void, custom item, adjustment, checkout/settle/cancel은 D19~D21 범위로 남긴다. 주문 탭 mutation은 `expectedVersion`으로 stale update 409를 반환하고, 모든 bar-scoped 접근은 active membership과 `can_manage_orders` 권한을 검사한다. 기기별 URL, 화면 폭 redirect, production secret, remote push, production deploy는 수행하지 않는다.
차단 요소: 없음
완료 항목: 작업 시작, D18 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-19/WF-20/WF-05 compact·medium·wide 기준 확인, order tab contract/status/query/create/update/detail/event 추가, `0014_d18_order_tabs.sql` migration 추가, `order_tab_counters`/`order_tabs`/`order_tab_events` schema 추가, memory/D1 order tab repository 추가, order tab service의 active membership·`can_manage_orders` guard와 `expectedVersion` 409 충돌 처리 구현, `/api/bars/{barId}/order-tabs` list/create와 `/api/bars/{barId}/order-tabs/{tabId}` detail/update route 추가, `/bars/{barId}/orders` 목록·필터·생성 UI와 `/bars/{barId}/orders/{orderTabId}` 상세·수정 UI 구현, 헤더 현재 작업 바 selector 유지, 권한별 sidebar 주문 메뉴 노출 회귀 보강, full fixture 주문 탭 seed 추가, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `node scripts/verify-sqlite-migrations.mjs` 통과(15 migrations 빈 SQLite 적용), `npm run verify:migrations` 통과(15 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과(root 기준; admin workspace 직접 lint는 기존 script cwd 제약으로 `tsconfig.base.json` ENOENT), `npm run typecheck` 통과(admin/customer), `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts` 통과(3 tests), `npm run test` 통과(admin 17 files/125 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts` 통과(3 viewport tests), `npm run test:e2e --workspace admin-menu-manager -- preview.spec.ts` 통과(4 shell/sidebar regression tests), `npm run test:e2e --workspace admin-menu-manager` 통과(60 tests), `npm run build` 통과(admin/customer; admin Vite chunk size warning은 non-failing), 로컬 서버 `http://127.0.0.1:5173/` 실행 유지 및 full fixture reset 가능
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — order tabs `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-compact-chromium/orders-compact.png`, `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-medium-chromium/orders-medium.png`, `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-wide-chromium/orders-wide.png`. WF-19 목록, WF-20 상세, WF-05 헤더 바 선택·sidebar 기준을 확인했다.
와이어프레임 대비 차이 및 근거: WF-19의 주문 탭 목록, 상태 filter, 신규 탭 생성, 선택 상세 진입과 WF-20의 상세 메모·손님 표시 수정 구조를 같은 `/bars/{barId}/orders` 및 `/bars/{barId}/orders/{orderTabId}` route에 구현했다. 와이어프레임의 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 주문 라인/수량/void, 기타 항목, 금액 조정, 계산 요청 처리, 정산, 취소, 일별 요약은 D19~D21 범위라 D18에서는 구현하지 않았다.
resize 상태 보존: D18 E2E에서 `/bars/{barId}/orders/{orderTabId}` 상세 URL, 상세 손님 표시/메모 draft, 헤더 현재 작업 바 selector, 목록 filter/search state가 1440→390 resize 후 유지됨을 확인. compact·medium·wide 모두 불필요한 가로 스크롤 없음과 44px touch target을 확인했다.
DB migration: `admin-menu-manager/db/migrations/0014_d18_order_tabs.sql` 추가. `order_tab_counters`, `order_tabs`, `order_tab_events` table과 bar별 tab number unique/index, status/label/event index를 빈 SQLite에 순차 적용 검증 완료.
API/환경 변화: `/api/bars/:barId/order-tabs` GET/POST와 `/api/bars/:barId/order-tabs/:tabId` GET/PATCH 추가. local dev/test full fixture에 주문 탭 `A1`, `Bar 3`, `B2`를 seed한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음. 로컬 관리자 dev server는 `http://127.0.0.1:5173/`에서 실행 중이다.
인수 시나리오 결과: staff full fixture에서 헤더 바 selector로 Sample Bar를 유지한 채 주문 탭 메뉴에 접근하고, 기존 더미 탭 목록을 확인한 뒤 신규 탭을 생성, 상세를 수정, resize 후 draft를 보존하고 저장 성공 메시지를 확인했다. sidebar는 staff에게 주문 탭만 노출하고 메뉴, 카테고리, 바 회원, 사용자 계정 메뉴는 숨김을 확인했다.
남은 문제: D19 메뉴 주문 라인·수량·void 미착수. D20 기타 주문 항목·금액 조정, D21 계산 요청·정산·취소·일별 요약 미착수. D18은 탭 version 충돌까지만 구현했으며 주문 라인 생성 시점 금액 스냅샷, 주문 추가/settle idempotency, closed/cancelled 운영 흐름은 D19~D21에서 처리해야 한다.
완료 시각: 2026-06-23 16:37 KST
사람 검토 결과: 미검토

### 2026-06-23 15:42 KST — D17
상태: 완료
예상 변경 영역: publication snapshot republish contract/service/API, bar lifecycle deactivate/reactivate contract/service/API, fake GitHub delete/restore writes, fake Cloudflare deployment tracking reuse, `/bars/{barId}/publications` 재발행 action, `/bars/{barId}` active/inactive action, compact·medium·wide WF-18/WF-08 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-18 `/bars/{barId}/publications`, WF-08 `/bars/{barId}`, 참고 WF-06 `/bars`
시작 메모: 가장 앞선 미완료 업무 D17만 진행한다. 과거 성공 snapshot 재발행은 현재 D1 편집본을 바꾸지 않는다. inactive 전환은 고객 JSON 삭제 commit으로 고객 URL 404를 유도하고 D1 바·메뉴·발행 이력은 유지한다. active 복원은 마지막 성공 공개본이 있으면 복원하고 없으면 preparing 공개본을 복원한다. 실제 Cloudflare token, production deploy, remote push는 수행하지 않고 fake adapter와 local/test data로 검증한다.
차단 요소: 없음
완료 항목: 작업 시작, D17 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-18/WF-08 compact·medium·wide 원본 확인, publication operation contract에 `snapshot_republish`/`delete_menu_json`/`restore_snapshot`/`restore_preparing` 추가, `0013_d17_bar_lifecycle.sql` migration 추가, `bar_lifecycle_events` repository/schema 추가, fake GitHub `deleteFile` 추가, 과거 성공 snapshot 재발행 API/service 구현, current D1 편집본 비변경 검증, bar inactive JSON 삭제 commit 및 lifecycle event 구현, active restore snapshot/preparing 구현, `/bars/{barId}/publications` 재발행 action/확인 panel 구현, `/bars/{barId}` lifecycle action/dialog/event UI 구현, D13 헤더 바 선택·권한별 sidebar dev 화면 회귀 확인, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npx vitest run --config vitest.config.ts tests/integration/publicationsApi.test.ts` 통과(D15~D17 14 tests), `npm run test` 통과(admin 16 files/122 tests, customer 3 files/13 tests), `npm run verify:migrations` 통과(14 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과(root 기준; admin workspace cwd 직접 실행은 기존 script cwd 제약으로 `tsconfig.base.json` ENOENT), `npm run typecheck` 통과(admin/customer), `npx playwright test --config playwright.config.ts tests/e2e/publications.spec.ts tests/e2e/bars.spec.ts` 통과(11 tests), `npm run test:e2e` 통과(admin 57 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities, 로컬 서버 `http://127.0.0.1:5173/dashboard` 200 응답 확인, full fixture `POST /__dev/reset-auth?fixtures=full` 적용
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — publication `publication-compact.png`/`publication-medium.png`/`publication-wide.png`, bar lifecycle `bar-lifecycle-compact.png`/`bar-lifecycle-medium.png`/`bar-lifecycle-wide.png`. WF-18 원본 `/tmp/thebar-d17-wireframe-publications-compact.png`, `/tmp/thebar-d17-wireframe-publications-medium.png`, `/tmp/thebar-d17-wireframe-publications-wide.png`, WF-08 원본 `/tmp/thebar-d17-wireframe-bar-overview-compact.png`, `/tmp/thebar-d17-wireframe-bar-overview-medium.png`, `/tmp/thebar-d17-wireframe-bar-overview-wide.png` 확인.
와이어프레임 대비 차이 및 근거: WF-18의 발행 이력/상세 구조를 같은 `/bars/{barId}/publications` route에 유지하고 D17 범위인 과거 성공 snapshot 재발행 action을 추가했다. WF-08의 바 상세/진입점 구조를 유지하고 active/inactive lifecycle panel, 영향 설명, 확인 dialog/sheet, 최근 이벤트를 추가했다. 와이어프레임의 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다. 실제 GitHub/Cloudflare token, production deploy, remote push는 수행하지 않고 fake adapter로 target commit SHA와 파일 삭제/복원을 검증했다.
resize 상태 보존: D17 publication E2E에서 재발행 확인 panel이 390 resize 후 같은 `/bars/{barId}/publications` URL에서 유지되고 `snapshot_republish` 이력이 표시됨을 확인. D17 lifecycle E2E에서 비활성화 확인 dialog가 390 resize 후 같은 `/bars/{barId}` URL에서 유지되고 inactive 404 설명과 preparing 복원 결과가 같은 route에서 표시됨을 확인. D13 회귀 E2E에서 header 현재 작업 바 selector와 권한별 sidebar가 full fixture staff/admin에서 유지됨을 확인.
DB migration: `admin-menu-manager/db/migrations/0013_d17_bar_lifecycle.sql` 추가. 기존 `publications` table을 operation check 확장용으로 재작성하고 `bar_lifecycle_events` table/index를 추가했다. 빈 SQLite에 0000~0013 순차 적용 검증 완료.
API/환경 변화: `/api/bars/:barId/publications/:publicationId/republish` POST 추가. `/api/bars/:barId/lifecycle` POST 추가. local dev/test 전용 fake GitHub adapter에 delete operation을 추가했으며 production secret, 실제 GitHub/Cloudflare/production DB 연결 없음. 화면 테스트용 full fixture는 `admin1`/`owner1`/`manager1`/`staff1` 계정과 Sample Bar 데이터를 제공한다. 로컬 관리자 dev server는 `http://127.0.0.1:5173/`에서 실행 중이다.
남은 문제: D18 주문 탭 생성·목록·상세 미착수. D17은 과거 재발행과 바 수명주기까지만 완료했으며 주문 탭, 주문 금액 스냅샷, 주문 idempotency/version 처리는 수행하지 않았다.
완료 시각: 2026-06-23 16:11 KST
사람 검토 결과: 미검토

### 2026-06-23 15:26 KST — D16
상태: 완료
예상 변경 영역: publication deployment contract/status, Cloudflare fake adapter, publication repository retention/status update, D15 발행 service polling 확장, `/api/bars/{barId}/publications` status/history API, `/bars/{barId}/publications` 이력·상세·polling UI, compact·medium·wide WF-18 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-18 `/bars/{barId}/publications`, 참고 WF-05 `/dashboard`
시작 메모: 가장 앞선 미완료 업무 D16만 진행한다. 실제 Cloudflare token, production deploy, remote push는 수행하지 않고 fake adapter로 commit SHA 연계 deployment 조회, 대상 SHA 불일치 방지, 30초 polling API, 3분 timeout_unknown 기록, 성공/실패/확인 불가 이력과 snapshot 보관을 검증한다. 과거 snapshot 재발행과 바 수명주기는 D17 범위로 남긴다.
차단 요소: 없음
완료 항목: 작업 시작, D16 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-18 compact/medium/wide 원본 확인, publication contract에 `waiting_cloudflare`/deployment/polling/editDiff 추가, `0012_d16_publication_deployments.sql` migration 추가, fake Cloudflare adapter 추가, publication repository deployment field/list/prune 구현, service의 GitHub commit 후 `waiting_cloudflare` 전환과 target SHA deployment reconciliation 구현, 3분 `timeout_unknown` 기록, 성공 100건·실패/확인 불가 100건 보관, `/api/bars/{barId}/publications` polling 응답 확장, `/bars/{barId}/publications` 배포 상태·이력·상세 dialog/sheet·timeout 설명 UI 구현, medium 이하 card timeline 전환, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(13 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 16 files/118 tests, customer 3 files/13 tests), `npm run test --workspace admin-menu-manager -- tests/integration/publicationsApi.test.ts` 통과(D16 10 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/publications.spec.ts` 통과(D16 4 tests), `npm run test:e2e` 통과(admin 54 tests, customer 4 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities, 로컬 서버 `http://127.0.0.1:5173/dashboard` 200 응답 확인
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `publication-compact.png`/`publication-medium.png`/`publication-wide.png`. WF-18 원본도 `/tmp/thebar-wf18-compact.png`, `/tmp/thebar-wf18-medium.png`, `/tmp/thebar-wf18-wide.png`로 확인.
와이어프레임 대비 차이 및 근거: WF-18의 발행 이력, 단계, 상세 확인 구조를 같은 `/bars/{barId}/publications` route에 유지하고 D16 범위인 Cloudflare deployment 상태, 30초 polling, 최신 공개본과 편집본 차이, 상세 dialog/sheet, `timeout_unknown` 설명을 추가했다. wide는 history/table+detail, medium/compact는 card timeline과 dialog/sheet로 적응한다. 실제 Cloudflare token, production deploy, remote push는 수행하지 않고 fake adapter로 target commit SHA 판정을 검증했다. 과거 snapshot 재발행과 바 비활성/재활성은 D17 범위로 남겼다.
resize 상태 보존: D16 E2E에서 `/bars/{barId}/publications` 발행 확인 dialog가 390 resize 후 유지됨을 확인. D16 E2E에서 발행 후 상세 dialog/sheet, fake Cloudflare success, page reload 후 이력 복구를 확인. Vitest responsive shell에서 발행 확인 상태와 선택한 발행 이력이 1440→390 resize 후 유지됨을 확인. URL은 `/bars/{barId}/publications` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0012_d16_publication_deployments.sql` 추가. 기존 `publications` table을 새 status check와 deployment column으로 재작성하고 `publications_bar_deployment_idx`를 추가했다. 빈 SQLite에 0000~0012 순차 적용 검증 완료.
API/환경 변화: `/api/bars/:barId/publications` GET/POST 응답에 deployment, polling, editDiff 추가. local dev/test 전용 fake Cloudflare adapter를 추가했으며 production secret, 실제 GitHub/Cloudflare/production DB 연결 없음. 로컬 관리자 dev server는 `http://127.0.0.1:5173/`에서 실행 중이다.
남은 문제: D17 과거 재발행·바 수명주기 미착수. D16은 deployment 상태 추적과 이력 보관까지만 완료했으며 과거 성공 snapshot 재발행, 현재 편집본 비변경 검증, 바 비활성/재활성 처리는 수행하지 않았다.
완료 시각: 2026-06-23 15:39 KST
사람 검토 결과: 미검토

### 2026-06-23 15:05 KST — D15
상태: 완료
예상 변경 영역: publication contract/state, publications/snapshots/locks schema migration, fake GitHub adapter/repository/service/API, `/bars/{barId}/publications` UI, preview/menu 발행 action 연결, compact·medium·wide WF-18 검증, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-18 `/bars/{barId}/publications`, 참고 WF-17 `/bars/{barId}/preview`
시작 메모: 가장 앞선 미완료 업무 D15만 진행한다. 실제 production GitHub token, remote push, production deploy는 수행하지 않고 fake adapter로 create/update/동일 내용 trigger commit, 바별 lock과 고객 repo 전역 commit lock, schema 실패 시 commit 없음, 외부 오류 sanitization을 검증한다. Cloudflare deployment polling/이력 심화는 D16 범위로 남긴다.
차단 요소: 없음
완료 항목: 작업 시작, D15 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-18 compact/medium/wide 기준 확인, publication contract/status/operation/request/response 추가, `0011_d15_publications.sql` migration 추가, fake GitHub adapter create/update/SHA/commit log 구현, memory/D1 publication repository 추가, D13 public DTO builder 기반 발행 service 추가, 바별 발행 lock과 고객 repo 전역 commit lock 분리, 동일 내용 trigger file 갱신, schema 실패 no-commit, 외부 오류 sanitization, `/api/bars/{barId}/publications` GET/POST route 추가, `/bars/{barId}/publications` UI 구현, preview/menu 발행 action 연결, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(12 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 16 files/114 tests, customer 3 files/13 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/publications.spec.ts` 통과(D15 4 tests), `npm run test:e2e` 통과(admin 54 tests, customer 4 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities, 로컬 서버 `http://127.0.0.1:5173/dashboard` 200 응답 확인
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `publication-compact.png`/`publication-medium.png`/`publication-wide.png`
와이어프레임 대비 차이 및 근거: WF-18의 발행 대상, 확인 panel, 진행 단계, 최근 발행 이력, 스냅샷 상세를 단일 `/bars/{barId}/publications` route에 구현했다. WF-17 미리보기와 D12 메뉴 목록의 발행 버튼은 같은 발행 route로 이동한다. 실제 GitHub/Cloudflare 연결은 요구 범위상 fake adapter로만 검증했고 production token, remote push, production deploy는 수행하지 않았다. Cloudflare deployment polling과 배포 이력 심화는 D16 범위로 남겼다.
resize 상태 보존: D15 E2E에서 `/bars/{barId}/publications` 발행 확인 checkbox가 390 resize 후 유지됨을 확인. Vitest responsive shell에서 발행 확인 상태와 선택한 발행 이력이 1440→390 resize 후 유지됨을 확인. URL은 `/bars/{barId}/publications` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0011_d15_publications.sql` 추가. `publications`, `publication_snapshots`, `publication_locks`, `repository_commit_lock` table과 관련 index를 빈 SQLite에 순차 적용 검증 완료.
API/환경 변화: `/api/bars/:barId/publications` GET/POST 추가. local dev/test 전용 fake GitHub adapter는 `public/menus/{encodedSlug}.json`과 `public/publish-triggers/{encodedSlug}.json` 쓰기를 메모리에 기록한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
남은 문제: D16 Cloudflare 배포 상태·발행 이력 미착수. D15는 GitHub 발행 commit 준비와 fake adapter 검증까지만 완료했으며 Cloudflare deployment polling, timeout_unknown 후속 판정, 배포 이력 심화는 수행하지 않았다.
완료 시각: 2026-06-23 15:23 KST
사람 검토 결과: 미검토

### 2026-06-23 14:49 KST — D14
상태: 완료
예상 변경 영역: customer public JSON parser/schemaVersion/fixture tests, `/{encodedSlug}` 고객 UI, search/category/item expanded state, idle reset, loading/preparing/not-found/schema/network 상태, compact·medium·wide WF-22 E2E, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-22 `/{encodedSlug}`, 참고 WF-17 `/bars/{barId}/preview`
시작 메모: 가장 앞선 미완료 업무 D14만 진행한다. 고객 앱은 관리자 API·D1·주문/결제/admin link를 호출하거나 노출하지 않고 `/menus/{encodedSlug}.json`만 읽는다. 같은 URL에서 compact·medium·wide 레이아웃만 전환하며 검색·카테고리·펼침 상태는 resize 후 유지한다.
차단 요소: 없음
완료 항목: 작업 시작, D14 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, WF-22 compact/medium/wide 원본 확인, public JSON fetch/cache/credentials/source 정책 추가, schemaVersion·malformed·not-found·network 오류 구분, preparing/published/empty/sold_out fixture 테스트 보강, 고객 published fixture 확장, `/{encodedSlug}` 바 정보 접기/펼치기·검색·카테고리 탐색·메뉴 상세 펼침·가격/ABV/배지/템플릿 상세 UI 구현, 5분 idle UI reset 구현, shared renderer preview 회귀 유지, 고객 앱 관리자 API/주문/결제/로그인 링크 비노출 검증, E2E/인수, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(11 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 15 files/104 tests, customer 3 files/13 tests), `npm run test:e2e --workspace customer-menu-board` 통과(D14 4 tests), `npm run test:e2e` 통과(admin 50 tests, customer 4 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `customer-menu-compact.png`/`customer-menu-medium.png`/`customer-menu-wide.png`. WF-22 원본도 `/tmp/thebar-wf22-compact.png`, `/tmp/thebar-wf22-medium.png`, `/tmp/thebar-wf22-wide.png`로 확인.
와이어프레임 대비 차이 및 근거: WF-22의 바 정보, 검색, 카테고리 이동, 메뉴 상세 확인, 품절/빈 카테고리, compact 가로 chips, medium/wide rail 구조를 단일 `/{encodedSlug}` route에 구현했다. 와이어프레임의 저충실도 회색 표현은 브랜드 토큰, 44px touch target, 실제 public badge 색상으로 대체했다. 주문/결제/admin 링크는 요구사항상 고객 UI에서 금지되어 추가하지 않았다.
resize 상태 보존: D14 E2E에서 `/{encodedSlug}` 검색어 `맥`, 펼친 `맥캘란 12` 상세 field, 전체/카테고리 상태가 390 resize 후 유지됨을 확인. 5분 idle reset은 Vitest에서 URL 유지와 검색·펼침 상태 초기화를 확인.
DB migration: 추가 없음. 0000~0010 기존 migration을 수정하지 않고 빈 SQLite에 순차 적용 검증 완료.
API/환경 변화: 고객 앱 fetch는 `/menus/{encodedSlug}.json`만 사용하며 `cache: no-cache`, `credentials: omit`를 적용한다. `/api` JSON source 차단과 in-flight JSON 요청 dedupe를 추가했다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
남은 문제: D15 GitHub 발행 미착수. D14는 고객 정적 메뉴판 UI와 public JSON 읽기까지만 완료했으며 실제 발행 commit과 Cloudflare 배포 추적은 수행하지 않았다.
완료 시각: 2026-06-23 15:02 KST
사람 검토 결과: 미검토

### 2026-06-23 14:21 KST — D13
상태: 완료
예상 변경 영역: public menu contract/schema/DTO builder/canonical hash/preview API, `/bars/{barId}/preview` UI, 고객 renderer 재사용, 관리자 헤더 바 선택, 권한별 사이드바 필터, 로컬 화면 테스트 fixture, backend/frontend/E2E tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-17 `/bars/{barId}/preview`, 참고 WF-22 `/{encodedSlug}`
시작 메모: 가장 앞선 미완료 업무 D13만 진행한다. 사용자가 지적한 바 선택 header와 권한별 sidebar는 D13 preview 및 기존 bar-scoped 화면 접근에 필요한 관리자 shell 보강으로 함께 처리한다. 화면 테스트에 필요한 더미데이터는 local dev/test fixture에만 추가한다.
차단 요소: 없음
완료 항목: 작업 시작, D13 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, 공유 public menu Zod schema/parser/filter/renderer 추가, public DTO forbidden key scan과 canonical content hash 추가, `/api/bars/{barId}/preview` API 추가, hidden category/menu 제외와 sold_out 가격·배지 숨김 구현, 내부 ID·사용자 정보·내부 메모·token/secret 계열 field 비공개 검증, `/bars/{barId}/preview` UI 구현, 관리자 header 현재 작업 바 selector 추가, 권한별 sidebar 필터 구현, local full fixture 추가, 고객 fixture/renderer 갱신, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(11 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 15 files/104 tests, customer 2 files/4 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/preview.spec.ts` 통과(D13 4 tests), `npm run test:e2e` 통과(admin 50 tests, customer 3 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 확인 완료 — `preview-compact.png`/`preview-medium.png`/`preview-wide.png`, 고객 smoke `customer-menu-compact.png`/`customer-menu-medium.png`/`customer-menu-wide.png`
와이어프레임 대비 차이 및 근거: WF-17의 schema 상태, hash, 범위 선택, 고객 메뉴 검색, 실제 고객 메뉴판 미리보기를 단일 `/bars/{barId}/preview` route에 구현했다. WF-22는 shared renderer와 고객 fixture를 통해 참고 반영했지만 D14 고객 메뉴판 UX 고도화와 발행된 JSON 배포는 후속 범위로 남겼다. 와이어프레임의 header 바 선택 요구는 관리자 AppShell 현재 작업 바 selector로 반영했고, 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다.
resize 상태 보존: D13 E2E에서 `/bars/{barId}/preview` 범위 선택, 고객 메뉴 검색어, 현재 작업 바 선택이 390 resize 후 같은 URL에서 유지됨을 확인. Vitest responsive shell에서도 preview 검색어와 바 선택 상태가 resize 후 유지됨을 확인.
DB migration: 추가 없음. 0000~0010 기존 migration을 수정하지 않고 빈 SQLite에 순차 적용 검증 완료.
API/환경 변화: `/api/bars/:barId/preview` 추가. local dev/test 전용 `/__dev/reset-auth?fixtures=full`로 Sample Bar, 카테고리, 메뉴, 가격, 상세, sold_out, hidden, 배지, `owner1`/`manager1`/`staff1` 화면 확인 데이터를 seed한다. production secret, 실제 GitHub/Cloudflare/production DB 연결 없음.
남은 문제: D14 고객 메뉴판 반응형 UI 미착수. D13은 미리보기와 shared renderer/fixture까지만 완료했으며 GitHub 발행(D15)과 Cloudflare 배포(D16)는 수행하지 않았다.
완료 시각: 2026-06-23 14:46 KST
사람 검토 결과: 미검토

### 2026-06-23 13:50 KST — D12
상태: 완료
변경 영역: `admin-menu-manager` menu list query/filter/bulk contract, menu badge assignment/reorder, repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-15 `/bars/{barId}/menus`, 참고 WF-16 `/bars/{barId}/menus/{menuItemId}`
완료 항목: 작업 시작, D12 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, menu list query/filter/bulk contract 추가, `0010_d12_menu_badge_assignments.sql` migration 추가, `menu_item_badges` FK·unique order 강화, memory/D1 menu item repository badge/list bulk 변경 구현, BadgeRepository usage sink 연결, service/API에 `/api/bars/{barId}/menu-items` query filter와 `/bulk` mutation 추가, `/bars/{barId}/menus` 전체 목록·카테고리 보기·wide table·compact card·필터·선택·quick edit·order move·bulk panel·badge selection/reorder UI 구현, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(11 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과(admin/customer), `npm run test` 통과(admin 14 files/97 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 46 tests, customer 3 tests), `npm run build` 통과(admin/customer), `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `d12-bulk-draft-compact.png`/`d12-bulk-draft-medium.png`/`d12-bulk-draft-wide.png`, `d12-bulk-saved-compact.png`/`d12-bulk-saved-medium.png`/`d12-bulk-saved-wide.png`
와이어프레임 대비 차이 및 근거: WF-15의 전체 목록·카테고리 보기·검색/필터·선택·일괄 변경·저장 전 초안·저장 action을 단일 `/bars/{barId}/menus` route에 구현했다. WF-15의 저충실도 회색 table/card 표현은 공통 디자인 토큰, badge chip 색상, 44px touch target 기준으로 대체했다. WF-16은 상세 화면 참고로만 사용했으며 D12에서는 기존 D11 상세 편집 route를 중복 구현하지 않았다. 고객 public DTO/JSON에서 hidden 제외와 sold_out 배지 숨김의 실제 출력은 D13 범위라, D12에서는 admin 저장 데이터와 sold_out 공개 시 배지 숨김 기준 문구·테스트 기반만 마련했다.
resize 상태 보존: D12 E2E에서 `/bars/{barId}/menus` 선택 2개, 일괄 품절·숨김·카테고리 이동·추천 배지 draft가 390 resize 후 URL·선택·미저장 변경 수를 유지함을 확인. Vitest responsive shell에서 검색어, 품목 유형 필터, 배지 필터, 선택 checkbox, quick sale draft, bulk category draft가 1440→390 resize 후 유지됨을 확인.
DB migration: `admin-menu-manager/db/migrations/0010_d12_menu_badge_assignments.sql` 추가. 기존 적용 migration은 수정하지 않고 `menu_item_badges`를 FK 포함 table로 재작성, orphan assignment 제거 copy, `bar_id/menu_item_id/display_order` unique index 추가, system/bar badge unique/index 유지. 빈 SQLite에 0000~0010 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 14:18 KST
남은 문제: D13 Public DTO·JSON Schema·미리보기 미착수. D12는 admin 저장과 목록 관리까지만 완료했으며 public JSON 생성·GitHub 발행·고객 렌더링은 후속 업무에서 구현 필요.
사람 검토 결과: 미검토

### 2026-06-23 13:30 KST — D11
상태: 완료
변경 영역: `admin-menu-manager` menu price/detail/internal memo contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-16 `/bars/{barId}/menus/{menuItemId}`, 참고 WF-22 `/{encodedSlug}`
완료 항목: 작업 시작, D11 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, price/detail/internal memo contract 추가, `0009_d11_menu_item_details.sql` migration 추가, Drizzle schema 추가, memory/D1 repository 확장, service 권한·가격·상세 상태 전이 구현, `/api/bars/{barId}/menu-items` create/update/read DTO 확장, `/bars/{barId}/menus/new`와 `/bars/{barId}/menus/{menuItemId}` 가격·상세·내부 메모 UI 구현, backend/frontend/E2E tests, README/검증결과/진행 문서 갱신
검증 명령 및 결과: `npm run verify:migrations` 통과(10 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run test --workspace admin-menu-manager -- tests/contracts/menuItemDetails.test.ts tests/integration/menuItemsApi.test.ts` 통과(7 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/contracts/menuItemDetails.test.ts tests/integration/menuItemsApi.test.ts` 통과(43 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/menu-items.spec.ts` 통과(D11 3 viewport tests), `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 14 files/94 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 43 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `menu-new-compact.png`/`menu-new-medium.png`/`menu-new-wide.png`, `menus-list-compact.png`/`menus-list-medium.png`/`menus-list-wide.png`, `menus-complete-compact.png`/`menus-complete-medium.png`/`menus-complete-wide.png`
와이어프레임 대비 차이 및 근거: WF-16의 가격 섹션, 상세 템플릿 동적 form, 내부 메모 read/write 상태, sticky save/revert/delete, template 변경 warning을 기존 단일 메뉴 상세 route에 구현했다. WF-16의 배지 assignment와 D12 목록 일괄 수정은 다음 업무 범위라 구현하지 않았다. WF-22 public JSON/customer render는 D13/D14 범위라 내부 메모 비공개 contract만 유지했다. 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다.
resize 상태 보존: D11 E2E에서 `/bars/{barId}/menus/new` 작성 중 390 resize 후 URL·메뉴 이름·ABV·가격 금액·상세 brand·내부 메모 draft 유지 확인. `/bars/{barId}/menus/{menuItemId}` 수정 중 URL·메뉴 이름·노출 checkbox·가격 금액·상세 brand·내부 메모 draft 유지 확인. Vitest responsive shell에서 `/bars/bar-1/menus/menu-1` 가격·상세·메모 draft가 1440→390 resize 후 유지됨을 확인.
DB migration: `admin-menu-manager/db/migrations/0009_d11_menu_item_details.sql` 추가. `menu_items.internal_memo`, `menu_item_prices`, `menu_item_details`, price label unique/index, detail template enum/JSON validation을 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 13:47 KST
남은 문제: D12 메뉴 목록·검색·일괄 수정·배지 미착수. D11에서는 배지 assignment/public DTO/customer render/publish/order 기능을 의도적으로 구현하지 않았다.
사람 검토 결과: 미검토

### 2026-06-23 13:10 KST — D10
상태: 완료
변경 영역: `admin-menu-manager` menu item contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-15 `/bars/{barId}/menus`, WF-16 `/bars/{barId}/menus/{menuItemId}`, 참고 WF-14 `/bars/{barId}/categories`
완료 항목: 작업 시작, D10 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, menu item contract 추가, `menu_items` schema 및 migration 추가, memory/D1 menu item repository 추가, D09 category usage와 D07 item type usage 실제 menu count 연결, menu item service와 active membership read 및 `canEditMenu` mutation guard 추가, `/api/bars/{barId}/menu-items` CRUD route 추가, `/bars/{barId}/menus`, `/bars/{barId}/menus/new`, `/bars/{barId}/menus/{menuItemId}` UI 구현, 바 개요 D10 진입점 활성화, dev fixture reset D10 repository 연결, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(9 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run test --workspace admin-menu-manager -- tests/integration/menuItemsApi.test.ts` 통과(3 tests), `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/integration/menuItemsApi.test.ts` 통과(39 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/menu-items.spec.ts` 통과(3 viewport tests), `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 13 files/90 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 43 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `menu-new-compact.png`/`menu-new-medium.png`/`menu-new-wide.png`, `menus-list-compact.png`/`menus-list-medium.png`/`menus-list-wide.png`, `menus-complete-compact.png`/`menus-complete-medium.png`/`menus-complete-wide.png`
와이어프레임 대비 차이 및 근거: WF-15의 메뉴 목록·검색/필터·상세 진입, WF-16의 기본 정보 편집·상태 toggles·sticky save/revert·delete confirm을 단일 route 계열로 구현. 가격, 상세 템플릿 필드, 내부 메모, 배지 일괄 편집은 D11/D12 범위라 D10에서는 이름·설명·카테고리·품목 유형 선택·판매 상태·노출·ABV까지만 구현했다. 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다.
resize 상태 보존: D10 E2E에서 `/bars/{barId}/menus/new` 작성 중 URL·메뉴 이름·ABV draft가 390 resize 후 유지됨을 확인. `/bars/{barId}/menus/{menuItemId}` 수정 중 URL·메뉴 이름·노출 checkbox draft 유지 확인. `/bars/{barId}/menus` 목록 검색어 유지 확인. Vitest responsive shell에서 목록 검색어·판매 상태 필터·선택 메뉴, 상세 form 이름·ABV·노출 checkbox draft가 1440→390 resize 후 유지됨을 확인.
DB migration: `admin-menu-manager/db/migrations/0008_d10_menu_items.sql` 추가. `menu_items` table, bar별 `menu_N` public id, category/item type FK, normalized name unique, category order index, sale/visibility index를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 13:29 KST
남은 문제: D11 가격·상세 템플릿·내부 메모 미착수. D10에서는 금액, 상세 template field 저장, 내부 메모, 배지 assignment UI를 의도적으로 구현하지 않았다.
사람 검토 결과: 미검토

### 2026-06-23 12:53 KST — D09
상태: 완료
변경 영역: `admin-menu-manager` category contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-14 `/bars/{barId}/categories`, 참고 WF-15 `/bars/{barId}/menus`, WF-22 `/{encodedSlug}`
완료 항목: 작업 시작, D09 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, category contract 추가, `categories` schema 및 migration 추가, memory/D1 category repository 추가, category service와 system-admin 및 `canEditMenu` guard 추가, `/api/bars/{barId}/categories` 조회·생성·수정·이동·정렬·삭제 route 추가, `/bars/{barId}/categories` UI 구현, 바 개요 D09 진입점 활성화, dev fixture reset D09 repository 연결, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(8 migrations 빈 SQLite 적용), `npm run test --workspace admin-menu-manager -- tests/integration/categoriesApi.test.ts tests/e2e/responsiveShell.test.tsx` 통과(31 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/categories.spec.ts` 통과(3 viewport tests), `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 12 files/79 tests, customer 2 files/4 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `category-form-compact.png`/`category-form-medium.png`/`category-form-wide.png`, `categories-complete-compact.png`/`categories-complete-medium.png`/`categories-complete-wide.png`
와이어프레임 대비 차이 및 근거: WF-14의 카테고리 구조, 선택 편집, 노출/숨김, 상위 이동, drag/drop과 버튼 fallback을 `/bars/{barId}/categories` 단일 route로 구현. WF-15 메뉴 CRUD와 WF-22 고객 메뉴판 실제 공개 JSON 렌더링은 D10/D13/D14 범위라 D09에서는 category reference와 hidden/description flag 관리까지만 구현했다. 저충실도 회색 스타일은 공통 디자인 토큰과 44px touch target 기준으로 대체했다.
resize 상태 보존: D09 E2E에서 카테고리 이름 draft가 390 resize 후 유지됨을 확인. Vitest responsive shell에서 `/bars/bar-1/categories` 검색어, 선택 카테고리, 카테고리 이름 draft가 1440→390 resize 후 유지됨을 확인. URL은 `/bars/{barId}/categories` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0007_d09_categories.sql` 추가. `categories` table, parent self FK, bar별 public id unique, root/parent별 normalized name unique, parent/order index를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 13:10 KST
남은 문제: D10 메뉴 기본 CRUD 미착수. D09의 menu usage count guard는 D10 `menu_items` table과 실제 메뉴 연결 후 실사용 count로 이어져야 함.
사람 검토 결과: 미검토

### 2026-06-23 12:30 KST — D08
상태: 완료
변경 영역: `admin-menu-manager` badge color/system badge/bar badge contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-13 `/system/badges`, 참고 WF-16 `/bars/{barId}/menus/{menuItemId}`, WF-22 `/{encodedSlug}`
완료 항목: 작업 시작, D08 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, badge color/system badge/bar badge contract 추가, `badge_colors`/`system_badges`/`bar_badge_visibility`/`bar_badges`/`menu_item_badges` schema 및 migration 추가, memory/D1 badge repository 추가, badge service와 system-admin 및 `canEditMenu` guard 추가, `/api/system/badges`, `/api/system/badge-colors`, `/api/bars/{barId}/badges` route 추가, `/system/badges` UI 구현, dev fixture reset D08 repository 연결, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(7 migrations 빈 SQLite 적용), `npm run test --workspace admin-menu-manager -- tests/integration/badgesApi.test.ts` 통과(4 tests), `npm run test --workspace admin-menu-manager -- tests/integration/badgesApi.test.ts tests/e2e/responsiveShell.test.tsx` 통과(28 tests), `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/badges.spec.ts` 통과(3 viewport tests), `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 11 files/72 tests, customer 2 files/4 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `badge-color-form-compact.png`/`badge-color-form-medium.png`/`badge-color-form-wide.png`, `badges-complete-compact.png`/`badges-complete-medium.png`/`badges-complete-wide.png`
와이어프레임 대비 차이 및 근거: WF-13의 시스템 공통 배지, Sample Bar 전용, 허용 색상, 미리보기 구조를 `/system/badges` 단일 route 내부 tab과 responsive grid로 구현. WF-16 메뉴 상세의 실제 배지 assignment UI와 WF-22 고객 메뉴판 노출은 D12/D14 범위라 `menu_item_badges` DB/API 준비와 관리 화면까지만 구현했다. 저충실도 회색 스타일은 공통 디자인 토큰과 실제 대비 계산 preview로 대체했다.
resize 상태 보존: D08 E2E에서 허용 색상 추가 form의 HEX draft와 선택 tab이 390 resize 후 유지됨을 확인. Vitest responsive shell에서 `/system/badges` 공통 배지 검색어, 선택 바, 색상 tab, HEX draft가 1440→390 resize 후 유지됨을 확인. URL은 `/system/badges` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0006_d08_badges.sql` 추가. badge color, system badge, bar badge visibility, bar badge, menu item badge assignment prep table과 unique/index, 기본 색상/공통 배지 seed를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 12:50 KST
남은 문제: D09 카테고리 관리 미착수. D08의 menu item badge assignment 실제 편집 연결은 D12 메뉴 목록·검색·일괄 수정·배지에서 수행 필요. 고객 메뉴판의 sold_out 배지 숨김 실제 렌더링은 D14에서 public DTO와 함께 검증 필요.
사람 검토 결과: 미검토

### 2026-06-23 12:04 KST — D07
상태: 완료
변경 영역: `admin-menu-manager` item type/template/grape variety contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-12 `/system/item-types`, 참고 WF-16 `/bars/{barId}/menus/{menuItemId}`
완료 항목: 작업 시작, D07 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, item type/template/grape contract 추가, `system_item_types`/`bar_item_types`/`bar_item_type_overrides`/`grape_varieties`/`grape_variety_candidates` schema 및 migration 추가, memory/D1 item type repository 추가, item type service와 권한 guard 추가, `/api/system/item-types`, `/api/bars/{barId}/item-types`, `/api/system/grape-varieties`, `/api/system/grape-variety-candidates` route 추가, `/system/item-types` UI 구현, dev fixture reset D07 repository 연결, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(6 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 10 files/64 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 34 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `item-type-common-form-compact.png`/`item-type-common-form-medium.png`/`item-type-common-form-wide.png`, `item-types-complete-compact.png`/`item-types-complete-medium.png`/`item-types-complete-wide.png`
와이어프레임 대비 차이 및 근거: WF-12의 시스템 공통, Sample Bar 전용, 포도 품종 후보 tab 구조를 `/system/item-types` 단일 route 내부 탭으로 구현. fixed template field는 요구사항대로 코드/contract 고정 정보로 표시만 하고 admin UI에서 동적 schema 편집은 제공하지 않는다. 바 전용 관리는 별도 device/page route를 만들지 않고 접근 가능한 바 선택과 같은 API state로 처리한다. WF-16 메뉴 상세의 포도 품종 사용은 D10~D11 범위라 승인 목록과 후보 승인까지만 구현했다.
resize 상태 보존: D07 E2E에서 공통 유형 생성 form의 유형 이름과 price label draft가 390 resize 후 유지됨을 확인. Vitest responsive shell에서 `/system/item-types` 검색어, 선택 유형, price label draft가 1440→390 resize 후 유지됨을 확인. URL은 `/system/item-types` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0005_d07_item_types.sql` 추가. system/bar item type, bar override, grape variety/candidate table과 unique/index, 기본 공통 유형 seed를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 12:28 KST
남은 문제: D08 배지·색상 미착수. D07의 item type 선택과 template detail field 실제 메뉴 CRUD 연결은 D10~D11에서 수행 필요. D1 usage count는 메뉴 table이 생기는 D10 이후 실제 menu reference count로 연결 필요.
사람 검토 결과: 미검토

### 2026-06-23 11:47 KST — D06
상태: 완료
변경 영역: `admin-menu-manager` bar settings/profile/hours/links/currency contract/DB/repository/service/API/UI/tests, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-11 `/bars/{barId}/settings`, 참고 WF-22 `/{encodedSlug}`
완료 항목: 작업 시작, D06 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, bar settings contract 추가, `bars` 공개 profile fields와 `bar_business_hours`/`bar_links` schema 및 migration 추가, memory/D1 repository settings read/update 추가, BarService settings read/update와 active membership + `canEditMenu` guard 추가, 통화 변경 system-admin 제한 추가, `/api/bars/{barId}/settings` GET/PATCH route 추가, `/bars/{barId}/settings` UI 구현, 바 개요 D06 진입점 활성화, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(5 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 9 files/56 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 31 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `bar-settings-form-compact.png`/`bar-settings-form-medium.png`/`bar-settings-form-wide.png`, `bar-settings-saved-compact.png`/`bar-settings-saved-medium.png`/`bar-settings-saved-wide.png`
와이어프레임 대비 차이 및 근거: WF-11의 기본 정보, 영업시간, 외부 링크, 운영 설정 section과 저장/되돌리기 action을 구현. wide는 2열 form/grid, compact는 1열 section과 요일 details, sticky action으로 적응한다. WF-11의 운영 설정 중 D06 범위인 통화만 구현하고 direct publish 같은 발행 설정은 D15 이후 발행 업무로 남겼다. 저장 성공 후에도 발행 전 고객 JSON 유지 메시지를 표시한다.
resize 상태 보존: D06 E2E에서 바 이름, 월요일 영업시간, 링크 reorder 상태가 390 resize 후 유지됨을 확인. Vitest responsive shell에서 `/bars/bar-1/settings` 바 이름과 링크 URL 미저장 입력이 1440→390 resize 후 유지됨을 확인. URL은 `/bars/{barId}/settings` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0004_d06_bar_settings.sql` 추가. `bars` profile/hash columns, `bar_business_hours`, `bar_links`, 관련 index를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 12:02 KST
남은 문제: D07 품목 유형·고정 템플릿·포도 품종 미착수. D06의 draft hash는 후속 D13/D15 public DTO·발행 업무에서 미발행 변경과 고객 JSON 생성에 연결 필요.
사람 검토 결과: 미검토

### 2026-06-23 11:22 KST — D05
상태: 완료
변경 영역: `admin-menu-manager` membership/role permission contract/DB/repository/service/API/UI/tests, dashboard accessible bars 연결, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-10 `/bars/{barId}/members`, 참고 WF-09 `/system/users`
완료 항목: 작업 시작, D05 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, membership contract 추가, `bar_memberships`/`bar_role_permissions` schema 및 migration 추가, 기존/신규 바 기본 역할 권한 seed 추가, memory/D1 membership repository 추가, membership service와 current permission guard 추가, `/api/bars/{barId}/members` 목록·추가·역할 변경·비활성화 route 추가, `/api/bars/{barId}/role-permissions` 조회·저장 route 추가, `/api/bars/{barId}/current-permissions` route 추가, dashboard/bar detail active membership 접근 연결, `/bars/{barId}/members` UI 구현, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(4 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 8 files/48 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 27 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `member-add-compact.png`/`member-add-medium.png`/`member-add-wide.png`, `members-list-compact.png`/`members-list-medium.png`/`members-list-wide.png`
와이어프레임 대비 차이 및 근거: WF-10의 회원 목록, 회원 추가, 선택 회원 편집, 역할별 권한 matrix를 구현. wide는 목록+side editor, compact는 카드 목록+full-screen editor/dialog, medium은 단일 column 패널로 적응한다. 회색 저충실도 스타일은 공통 디자인 토큰으로 대체했다. 메뉴 편집/주문 운영 권한의 실제 기능 적용은 D10/D18 이후 업무에서 연결한다.
resize 상태 보존: D05 E2E에서 회원 추가 dialog의 검색어·선택 사용자·역할이 resize 후 유지됨을 확인. 역할별 권한 checkbox draft도 resize 후 유지되고 같은 mutation으로 저장됨을 확인. Vitest responsive shell에서 `/bars/bar-1/members` 선택 회원과 `staff 메뉴 편집` checkbox draft가 1440→390 resize 후 유지됨을 확인. URL은 `/bars/{barId}/members` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0003_d05_memberships.sql` 추가. `bar_memberships`, `bar_role_permissions`, bar/user unique index, role permission 기본 seed를 빈 SQLite에 순차 적용 검증 완료. 신규 바 생성 시 D1 repository가 role permission 기본값을 함께 생성한다.
환경 변수: 추가 없음. production secret 없음. 실제 GitHub/Cloudflare/production DB 연결 없음.
완료 시각: 2026-06-23 11:45 KST
남은 문제: D06 바 기본 정보·영업시간·링크·통화 미착수. 권한 guard가 허용한 실제 메뉴/주문 mutation 적용은 D10/D18 이후 각 업무에서 연결 필요.
사람 검토 결과: 미검토

### 2026-06-23 11:08 KST — D04
상태: 완료
변경 영역: `admin-menu-manager` user management contract/API/service/repository/UI/tests, dashboard user quick action 연결, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-09 `/system/users`, 참고 WF-10 `/bars/{barId}/members`
완료 항목: 작업 시작, D04 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, system user contract 추가, auth repository managed user 목록/세션 집계 확장, system user service 추가, `/api/system/users` 목록·생성·상세와 activate/deactivate/unlock/reset-password command 추가, dashboard 사용자 생성 quick action 활성화, `/system/users` UI 구현, temporary password 1회 안내, destructive confirmation, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(3 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 7 files/39 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 23 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `user-create-compact.png`/`user-create-medium.png`/`user-create-wide.png`, `users-list-compact.png`/`users-list-medium.png`/`users-list-wide.png`
와이어프레임 대비 차이 및 근거: WF-09의 검색·상태 필터·생성·잠금 해제·비활성·임시 비밀번호 1회 안내 구조를 구현. WF-10 바 소속 정보는 D05 범위라 실제 membership을 추측하지 않고 `D05 이후 바 소속 배정`으로 표시함.
resize 상태 보존: D04 E2E에서 `/system/users` 생성 dialog 아이디 입력값이 390↔1440 resize 후 유지됨을 확인. 검색어·상태 필터·선택 사용자 요약도 resize 후 유지됨. URL은 `/system/users` 그대로 유지.
DB migration: 신규 migration 없음. D01의 `users`/`sessions` schema로 상태, 잠금, 강제 변경, 세션 revoke를 처리하며 0000→0002 빈 SQLite 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 임시 비밀번호는 응답 직후 1회 표시만 수행하고 저장·재조회하지 않음.
완료 시각: 2026-06-23 11:20 KST
남은 문제: D05 바별 회원·역할·권한 미착수. 실제 바 소속 표시와 역할별 권한 편집은 D05에서 연결 필요
사람 검토 결과: 미검토

### 2026-06-23 10:48 KST — D03
상태: 완료
변경 영역: `admin-menu-manager` bar contract/DB/repository/service/API/UI/tests, D02 dashboard summary 연결, root migration 검증 스크립트, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-06 `/bars`, WF-07 `/bars/new`, WF-08 `/bars/{barId}`, 참고 WF-11 `/bars/{barId}/settings`
완료 항목: 작업 시작, D03 범위 잠금, 요구사항/설계/개발계획/wireframes/진행상태 재확인, bar contract 추가, `bars`/`bar_public_counters` schema 및 migration 추가, memory/D1 repository 추가, system-admin bar service와 slug/encodedSlug 생성 추가, `/api/bars` 및 `/api/bars/{barId}` route 추가, dashboard bar summary 실제 repository 연결, `/bars` 목록·검색·필터·선택 UI 구현, `/bars/new` 생성 form 구현, `/bars/{barId}` 상세 UI 구현, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과(3 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 6 files/28 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 19 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 및 육안 확인 완료 — `bars-list-compact.png`/`bars-list-medium.png`/`bars-list-wide.png`, `bar-new-compact.png`/`bar-new-medium.png`/`bar-new-wide.png`, `bar-detail-compact.png`/`bar-detail-medium.png`/`bar-detail-wide.png`
와이어프레임 대비 차이 및 근거: WF-06의 목록·검색·필터·선택, WF-07의 서버 생성 slug/고객 경로 안내, WF-08의 상태·식별자·진입점·최근 발행 구조를 구현. WF-11 바 설정은 D06 범위라 D03 상세의 비활성 진입점으로만 노출함.
resize 상태 보존: D03 E2E에서 `/bars/new` form name/currency가 390↔1440 resize 후 유지됨을 확인. `/bars` 검색어·상태 필터·선택 바 요약도 resize 후 유지됨. URL은 `/bars`, `/bars/new`, `/bars/{barId}` 그대로 유지.
DB migration: `admin-menu-manager/db/migrations/0002_d03_bars.sql` 추가. `bars`, `bar_public_counters`, slug/encoded_slug unique index와 status index를 빈 SQLite에 순차 적용 검증 완료.
환경 변수: 추가 없음. production secret 없음. 로컬 dev fixture와 in-memory repository만 사용.
완료 시각: 2026-06-23 11:06 KST
남은 문제: D04 사용자 계정 관리 미착수. 바별 회원·권한(D05), 바 설정(D06), 메뉴/발행/주문(D09 이후, D15, D18 이후)은 후속 업무에서 연결 필요
사람 검토 결과: 미검토

### 2026-06-23 10:33 KST — D02
상태: 완료
변경 영역: `admin-menu-manager` dashboard contract/service/API/UI/E2E, app shell responsive polish, README/검증결과/진행 문서
참조 와이어프레임 ID 및 URL: WF-05 `/dashboard`, 참고 WF-08 `/bars/{barId}`, WF-19 `/bars/{barId}/orders`
완료 항목: 작업 시작, D02 범위 잠금, 로컬 서버 `http://127.0.0.1:5173/dashboard` 최신 코드로 재시작 및 응답 확인, dashboard contract 추가, dashboard service 추가, auth repository 사용자 상태 summary 추가, `/api/dashboard` route 추가, `/dashboard` UI를 API 응답에 연결, system-admin/bar-user variant 및 unavailable 위젯 표시 구현, 대시보드 로그아웃 복구, dev-only `admin1`/`staff1`/`forced1` fixture 추가, D02 E2E 6개 추가
검증 명령 및 결과: `npm run verify:migrations` 통과(2 migrations 빈 SQLite 적용), `npm run verify:no-device-routes` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 5 files/22 tests, customer 2 files/4 tests), `npm run test:e2e` 통과(admin 15 tests, customer 3 tests), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 완료 — admin system-admin `dashboard-admin-compact.png`/`dashboard-admin-medium.png`/`dashboard-admin-wide.png`, bar-user `dashboard-staff-compact.png`/`dashboard-staff-medium.png`/`dashboard-staff-wide.png`
와이어프레임 대비 차이 및 근거: WF-05의 운영 요약, 접근 가능한 바, 빠른 작업, 최근 알림 구조를 구현. D03/D05/D10/D15/D18 전 데이터는 추측 생성하지 않고 unavailable 또는 empty 상태로 명시함.
resize 상태 보존: D02 E2E에서 `/dashboard` URL 유지와 `선택된 바 없음` empty selection 상태의 390↔1440 resize 보존 확인. D03 전 실제 선택 가능한 바가 없어 선택 바 변경 보존은 empty-state 보존으로 검증함.
DB migration: D02 신규 migration 없음. D03의 bars schema를 선행 구현하지 않고 기존 auth 데이터와 explicit unavailable summary로 처리.
환경 변수: production env 추가 없음. 로컬 dev fixture만 `/__dev/reset-auth`로 초기화되며 실제 secret 없음.
남은 문제: 실제 바 목록·발행·주문 집계는 D03/D05/D10/D15/D18 이후 연결 필요
사람 검토 결과: 미검토

### 2026-06-23 10:16 KST — D01
상태: 완료
변경 영역: `admin-menu-manager` 인증 contract/DB/server/UI/tests, root migration 검증 스크립트, 진행 문서
참조 와이어프레임 ID 및 URL: WF-01 `/setup`, WF-02 `/login`, WF-03 `/change-password`, WF-04 `/recovery`, 참고 WF-05 `/dashboard`
완료 항목: 작업 시작, D01 범위 잠금, auth contract/password policy schema 추가, `users`/`sessions` schema 및 `0001_d01_auth.sql` 추가, 빈 SQLite migration 검증 스크립트 추가, PBKDF2 password hash abstraction, memory/D1 auth repository, setup/recovery/login/session/change-password/logout service와 API route, CSRF mutation 검증, forced password gate, Auth UI route, dev server Hono auth API 연결, backend/frontend/E2E tests
검증 명령 및 결과: `npm run verify:migrations` 통과, `npm run lint` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 18/customer 4), `npm run test:e2e` 통과(admin 9/customer 3), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): WF-01~WF-04 auth screens render/input preserve screenshots 생성, forced password login→change→dashboard→logout screenshots 생성
와이어프레임 대비 차이 및 근거: WF-01~WF-04의 URL·폼 우선 구조·compact form focus·wide two-column 설명/폼 구조 구현. D02 대시보드 운영 위젯은 범위 밖이라 인증된 placeholder만 유지.
resize 상태 보존: `/login` 아이디 입력값 resize 후 유지, `/change-password` 새 비밀번호 입력값 resize 후 유지, URL 유지 확인
DB migration: `admin-menu-manager/db/migrations/0001_d01_auth.sql` 추가, 0000→0001 빈 SQLite 적용 검증 완료
환경 변수: `.env.example`의 `SETUP_TOKEN`, `ADMIN_RECOVERY_TOKEN`, `SESSION_COOKIE_NAME` 사용. 실제 secret 없음. 로컬 dev fixture는 production에 포함되지 않는 `/__dev/reset-auth`와 `forced1`만 사용.
남은 문제: D02 운영 대시보드는 미착수. 실제 D1 binding/Cloudflare secret 연결은 production 배포 업무 전까지 주장하지 않음.
사람 검토 결과: 미검토

### 2026-06-23 10:01 KST — D00
상태: 완료
변경 영역: `admin-menu-manager`, `customer-menu-board`, 루트 검증 스크립트, 진행 문서
참조 와이어프레임 ID 및 URL: WF-05 `/dashboard` 필수, WF-02 `/login` 및 WF-22 `/{encodedSlug}` 참고
완료 항목: 두 workspace 초기화, TypeScript strict/React/Vite 설정, Hono Pages Functions entry, Drizzle/D1 migration 구조, Zod API envelope, Vitest/Playwright, responsive AppShell, adaptive primitives, 고객 `/{encodedSlug}` shell, fake GitHub/Cloudflare adapter interface, D01 endpoint 미구현 확인
검증 명령 및 결과: `npm run lint` 통과, `npm run format` 통과, `npm run typecheck` 통과, `npm run test` 통과(admin 12/customer 4), `npm run test:e2e` 통과(admin 3/customer 3), `npm run build` 통과, `npm audit --omit=dev` 0 vulnerabilities
반응형 검증(390/768/1440): Playwright screenshot 생성 완료 — admin `test-results/admin-responsive-*`, customer `test-results/customer-responsive-*`
와이어프레임 대비 차이 및 근거: WF-05/WF-02/WF-22의 제품 URL·정보 계층·단일 route 적응 원칙만 D00 foundation 수준으로 구현. 실제 인증·대시보드·메뉴 기능은 D01 이후 업무 범위로 남김.
resize 상태 보존: admin `/dashboard` 선택 바 필터와 선택 row 유지, customer `/{encodedSlug}` 검색어 유지, URL 동일 확인
DB migration: `admin-menu-manager/db/migrations/0000_d00_foundation.sql` 추가. 제품 table은 D01 이후 업무에서 추가.
환경 변수: `admin-menu-manager/.env.example`, `customer-menu-board/.env.example`에 변수 이름만 문서화. 실제 secret 없음.
남은 문제: npm install 중 최신 dev dependency 일부가 Node 23.10.0에 대해 engine warning을 출력했으나 모든 gate는 통과. 운영 기능은 D01부터 수직 슬라이스로 구현 필요.
사람 검토 결과: 미검토

## 차단 사항

아직 없음.

## 결정 및 위험 기록

- 관리자와 고객 UI는 기기별 URL이나 별도 페이지 트리를 사용하지 않는다.
- 모든 프론트 업무는 같은 route에서 390×844, 768×1024, 1440×900을 함께 완료한다.
- 포도 품종은 승인 전 메뉴 사용 불가 정책을 유지하고 파일럿에서 병목을 측정한다.
- 품절 상태는 v1에서 저장 후 발행해야 고객 메뉴판에 반영된다.
- 실제 PAT/Cloudflare token/production DB는 Codex에 제공하지 않는다.

## 작업 기록 템플릿

```text
### YYYY-MM-DD — Dxx
상태:
변경 영역:
참조 와이어프레임 ID 및 URL:
완료 항목:
검증 명령 및 결과:
반응형 검증(390/768/1440):
와이어프레임 대비 차이 및 근거:
resize 상태 보존:
DB migration:
환경 변수:
남은 문제:
사람 검토 결과:
```
