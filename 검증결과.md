# 패키지 검증 결과

- 필수 문서와 `wireframes/` 파일 존재 확인
- `wireframes/app.js` Node.js 문법 검사 통과
- 와이어프레임 화면 ID WF-01~WF-22와 `화면맵.md` 매핑 일치
- 기존 기기별 제품 경로 `/app/`, `/t/`, `/m/` 미포함 확인
- 개발 업무 D00~D24가 `개발계획.md`와 `진행상태.md`에 존재함을 확인
- 문서에서 `AGENTS.md`가 와이어프레임 필수 참조를 요구함을 확인

브라우저에서 `wireframes/index.html`을 열어 화면별 Compact·Medium·Wide 표현을 검토할 수 있다.

## 2026-06-24 — 관리자 내비게이션·복구 CTA 보강

- sidebar를 기능별 그룹으로 유지하되 현장 사용성이 중요한 `주문 운영`을 가장 먼저 탐색할 수 있도록 그룹 순서를 조정
- 권한별 메뉴 노출은 유지: staff는 주문·미리보기 중심, manager는 메뉴 관리와 주문, system-admin은 시스템 관리까지 표시
- compact drawer와 medium sidebar에서 `주문 운영` 그룹과 `주문 탭` 링크가 먼저 표시되도록 DOM 순서를 조정해 키보드 탐색 순서도 함께 개선
- 로그인 화면에서 `관리자 복구` 버튼 제거. `/recovery` route와 `/api/recovery`는 요구사항상 유지하며, 토큰을 가진 운영자만 직접 URL로 진입하는 비상 경로로 둠

검증:

- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d23-quality.spec.ts tests/e2e/preview.spec.ts tests/e2e/auth-flow.spec.ts`: 17 tests 통과

반응형 증거:

- D23 route matrix: compact 390, medium 768, wide 1440 통과
- staff sidebar: 768 sidebar와 390 drawer에서 첫 그룹 `주문 운영`, `주문 탭` 링크 노출 확인
- auth flow: 로그인 화면 390, 768, 1440에서 `관리자 복구` 버튼 미노출 확인

## 2026-06-24 — 관리자 UI/UX 상용화 보강

- `DESIGN.md` 추가: THE BAR 브랜드, 관리자 IA, 리스트/그리드 원칙, 문구 규칙, responsive/접근성 기준을 repo-local 디자인 소스로 기록
- 로그인하지 않은 사용자가 보호 route의 앱 셸이나 개발 검증 화면을 보지 못하도록 보호 route guard를 앱 셸 바깥으로 이동
- 관리자 shell 브랜드를 THE BAR로 통일하고 header 현재 작업 바 selector, 권한별 sidebar, forced password route 보호를 재검증
- `data-table`/`data-card` 공통 스타일을 정리해 wide table과 compact card가 같은 데이터/상태/mutation을 공유하도록 통일
- 체크박스는 시각 크기를 줄이고 44px hitbox를 유지해 상용 UI 밀도와 터치 접근성을 함께 맞춤
- 품목 유형 추가 CTA는 클릭 후 편집 패널로 scroll/focus되어 “아무 반응 없음”처럼 보이지 않도록 개선
- `Bar Ops`, `done`, Dxx/WF 노출, raw role/status enum, 내부 error code, `preparing`, `production`, `schema valid`, `commit`, `snapshot`, `void` 등 개발 문구를 기본 UI에서 제거하거나 한국어 운영 문구로 치환
- publication/audit/bar/order/status 라벨 helper를 정리해 발행 번호, 반영 번호, 고객 메뉴판, 운영 배포, 취소 처리 등 상용 문구를 표시
- local full fixture seed를 직렬화해 화면 테스트용 더미 데이터가 동시 요청 중 깨지지 않도록 보강
- TUI Grid는 공식 제품과 React wrapper를 검토했으나 현재 wrapper peer range가 React 16/17 기준이라 React latest 앱에 즉시 추가하지 않음. 이번 보강은 repo-native grid/list 패턴으로 완료

검증:

- `npm run lint`: 통과. formatting guard와 single-route responsive guard 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx`: 49 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/auth-flow.spec.ts tests/e2e/dashboard.spec.ts tests/e2e/item-types.spec.ts tests/e2e/preview.spec.ts tests/e2e/publications.spec.ts tests/e2e/audit.spec.ts tests/e2e/d24-pilot-readiness.spec.ts tests/e2e/bars.spec.ts`: 40 tests 통과
- `npm run test`: admin 19 files/146 tests, customer 3 files/13 tests 통과
- `npm run test:e2e`: admin 79 tests, customer 5 tests 통과
- `npm run build`: admin, customer production build 통과

반응형 증거:

- 관리자 전체 E2E가 390, 768, 1440 폭에서 로그인, dashboard, bar selector, 권한별 sidebar, bar settings, menu items, item types, publications, orders, audit, system users를 검증하고 `admin-menu-manager/test-results/*`에 screenshot/trace evidence를 남김
- 고객 메뉴판 E2E가 390, 768, 1440 폭에서 같은 `/{encodedSlug}` route와 public JSON 안정성을 검증하고 `customer-menu-board/test-results/*`에 evidence를 남김
- responsiveShell/D23/D24 E2E에서 resize 후 URL, 현재 작업 바, 검색/필터, 선택, form draft, publication dialog, order state 보존 확인

## 2026-06-24 — D24 파일럿·운영 배포

- `GET /api/system/pilot-readiness` 추가: system-admin 전용 파일럿 readiness API
- `pilotReadiness` contract 추가: automated pass, action required, manual required gate를 구분
- `/system/audit`에 D24 파일럿 준비 패널 추가. 새 route를 만들지 않고 기존 운영 도구 URL을 유지함
- `docs/operations/pilot-runbook.md` 추가: preview/production D1 분리, secret checklist, private customer repo/Pages, migration backup/rollback, token rotation, monitoring/incident, device acceptance, release gate 절차
- `docs/operations/operator-training.md` 추가: owner/manager/staff 교육 흐름, bar selector, menu, publication, order, incident drill
- `docs/operations/pilot-feedback.md` 추가: defect ledger, P0/P1 open 0건, feedback log, follow-up backlog
- `npm run verify:pilot` 추가: D24 문서·contract·service·UI·secret 원문 미포함을 정적 검증
- local full fixture에 food system item type과 wine/food/cigar 대표 메뉴 데이터를 추가해 wine/whisky/cocktail/food/cigar coverage를 충족
- D24 readiness API는 active 바 2개, owner/manager/staff 권한 coverage, 대표 메뉴 template coverage, 주문 open/checkout/closed seed를 실제 repository 상태에서 읽음
- 실제 production secret 생성, remote push, production deploy, production migration은 수행하지 않음. production 배포 승인은 사람 확인 gate로 유지

검증:

- `npm run verify:pilot`: D24 산출물 검증 통과
- `npm run verify:security`: mutation CSRF와 rate-limited route 정적 검증 통과
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run verify:migrations`: 20 migrations 빈 SQLite DB에 적용 성공
- `npm audit --omit=dev`: production dependency 취약점 0건
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/pilotReadinessApi.test.ts`: 3 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx`: 49 tests 통과
- `npm run test`: admin 19 files/146 tests, customer 3 files/13 tests 통과
- `npm run build`: admin, customer production build 통과
- `npm run verify:performance`: admin main 283.0 KiB, customer main 265.5 KiB, admin lazy route chunks 14 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d24-pilot-readiness.spec.ts`: D24 pilot readiness 4 tests 통과
- `npm run test:e2e`: admin 79 tests, customer 5 tests 통과

반응형 증거:

- Pilot readiness 390: `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-compact-chromium/d24-pilot-readiness-compact.png`
- Pilot readiness 768: `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-medium-chromium/d24-pilot-readiness-medium.png`
- Pilot readiness 1440: `admin-menu-manager/test-results/d24-pilot-readiness-D24-pilot-readiness-panel-at-wide-chromium/d24-pilot-readiness-wide.png`

resize 상태 보존:

- D24 E2E에서 `/system/audit` URL이 1440→390→768 resize 후 유지됨을 확인
- 감사 로그 검색 `Cloudflare`와 작업 필터 `publication.requested`가 resize 후 유지됨을 확인
- D24 파일럿 준비 패널이 resize 후 계속 표시되고 `파일럿 시작 준비` 상태를 유지함
- compact·medium·wide 모두 불필요한 가로 스크롤 없음과 44px touch target 기준 통과

## 2026-06-24 — D23 보안·접근성·성능 강화

- `0019_d23_rate_limits.sql` migration 추가: `rate_limit_buckets`, scope/key hash unique index, expiry index
- setup, recovery, login, publication publish, order settle에 rate limit 적용
- rate limit key는 SHA-256 hash로 저장하고 username/IP/token 원문을 저장하지 않음
- order settle은 기존 idempotency record를 먼저 확인해 같은 key 재시도 응답을 rate limit보다 우선함
- `RATE_LIMITED` 429 오류에 `retryAfterSeconds`와 `scope` details를 포함함
- HTTPS session cookie와 CSRF cookie의 Secure/SameSite/HttpOnly 속성, 보안 header 회귀 테스트 추가
- mutation CSRF 정적 검증과 rate-limited route 정적 검증 추가
- admin route를 lazy loading으로 분리하고 performance budget 검증 추가
- `AdaptiveDialog` focus trap, Escape close, focus restore 구현
- D23 관리자 전체 route matrix, keyboard-only, 360px, 200% zoom, touch target, overflow, permission sidebar, bar selector E2E 추가
- customer menu Web Vitals baseline과 resize 중 JSON 중복 fetch 방지 E2E 추가
- local full fixture는 `Sample Bar`, `Whisky Lab` 두 바와 `admin1`/`owner1`/`manager1`/`staff1` 계정을 유지하고 reset 시 rate limit bucket도 초기화함

검증:

- `npm run verify:security`: mutation CSRF와 D23 rate-limited route 정적 검증 통과
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run verify:migrations`: 20 migrations 빈 SQLite DB에 적용 성공
- `npm audit --omit=dev`: production dependency 취약점 0건
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/authApi.test.ts tests/integration/orderTabsApi.test.ts tests/integration/publicationsApi.test.ts`: 33 tests 통과
- `npm run test`: admin 18 files/143 tests, customer 3 files/13 tests 통과
- `npm run build`: admin, customer production build 통과
- `npm run verify:performance`: admin main 283.0 KiB, customer main 265.5 KiB, admin lazy route chunks 14, route chunk budget 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/d23-quality.spec.ts`: D23 quality 6 tests 통과
- `npm run test:e2e --workspace customer-menu-board`: customer 5 tests 통과
- `npm run test:e2e`: admin 75 tests, customer 5 tests 통과

반응형 증거:

- Admin route matrix 390: `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-compact-chromium/d23-route-matrix-compact.png`
- Admin route matrix 768: `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-medium-chromium/d23-route-matrix-medium.png`
- Admin route matrix 1440: `admin-menu-manager/test-results/d23-quality-D23-major-admin-route-matrix-at-wide-chromium/d23-route-matrix-wide.png`

resize 상태 보존:

- manager가 `Whisky Lab`을 선택하고 category search `위스키`를 입력한 뒤 1440→390→768 resize 후 선택 바, search 값, URL이 유지됨을 확인
- resize 중 `/api/dashboard`와 `/api/current-permissions` 중복 fetch가 발생하지 않음을 확인
- customer menu는 390→768→1440→390 resize 후 같은 `/{encodedSlug}` URL과 단일 public JSON request를 유지함
- compact·medium·wide 모두 주요 관리자 route에서 가로 overflow 없음, 44px touch target 기준 통과

## 2026-06-24 — D22 감사 로그·보관·운영 도구

- `audit_logs`, `maintenance_runs` schema와 `0018_d22_audit_maintenance.sql` migration 추가
- 감사 로그 contract, memory/D1 audit repository, audit service, `/api/system/audit` 조회 API 추가
- `/api/system/audit/maintenance-runs` dry-run/execute API 추가
- 주문 closed/cancelled 365일, 일별 정산 요약 3년, 발행 성공/실패 이력 100건 초과 보관 정책 구현
- 로그인 실패와 주요 mutation route audit mapper 추가. 감사 기록 실패는 비즈니스 응답을 바꾸지 않음
- audit metadata에서 password, token, secret, session, csrf, memo, description, raw body 계열 key 저장 차단 확인
- `/system/audit` UI 구현: search, actor/bar/operation/result/date filters, wide table, compact event card, safe metadata detail, maintenance dry-run/execute panel
- system-admin sidebar에만 감사 로그 메뉴 노출, staff 직접 접근 403 화면 확인
- local full fixture에 D22 화면 확인용 감사 로그와 maintenance dry-run 샘플 추가
- D22 filter 입력 중 loading remount로 한 글자만 남던 문제를 테스트에서 발견하고 기존 데이터를 유지하는 background refresh로 수정
- compact에서 긴 error code badge가 가로 overflow를 만들던 문제를 E2E screenshot으로 발견하고 audit card badge wrapping으로 수정

검증:

- `npm run verify:migrations`: 18 migrations 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 18 files/139 tests, customer 3 files/13 tests 통과
- `npm run build`: admin, customer production build 통과. admin Vite 500KB chunk warning은 non-failing
- `npm run test:e2e -- audit.spec.ts`: D22 audit 5 tests 통과
- `npm run test:e2e`: admin 69 tests, customer 4 tests 통과
- 로컬 서버 `http://127.0.0.1:5173/system/audit` 200 응답 확인

반응형 증거:

- Audit 390: `admin-menu-manager/test-results/audit-D22-audit-log-page-at-compact-chromium/audit-compact.png`
- Audit 768: `admin-menu-manager/test-results/audit-D22-audit-log-page-at-medium-chromium/audit-medium.png`
- Audit 1440: `admin-menu-manager/test-results/audit-D22-audit-log-page-at-wide-chromium/audit-wide.png`

resize 상태 보존:

- D22 Playwright에서 `/system/audit` URL 유지, search `Cloudflare`, operation `publication.requested`, result `failure`, dry-run 완료 결과가 1440→390 resize 후 유지됨을 확인
- Vitest responsive shell에서 search, operation/result filters, 선택 이벤트 `테이블 4 정산`, maintenance dry-run 결과가 resize 후 유지됨을 확인
- compact·medium·wide 모두 가로 overflow 없음과 44px touch target 기준 통과

## 2026-06-23 — D00 프로젝트 기반과 반응형 앱 셸

- `admin-menu-manager`, `customer-menu-board` workspace 생성
- React + Vite + TypeScript strict + Vitest + Playwright 기반 설정
- 관리자 Hono Pages Functions entry, request ID, security headers, API envelope contract 확인
- Drizzle schema와 `0000_d00_foundation.sql` migration 구조 생성
- fake GitHub/Cloudflare adapter interface만 생성하고 실제 외부 호출 없음 확인
- 관리자 `/dashboard`, `/login` foundation route와 고객 `/{encodedSlug}` 단일 route smoke 확인
- `npm run lint` 통과: device-specific route, viewport redirect, mobile/desktop whole-page fork 없음
- `npm run typecheck` 통과
- `npm run test` 통과: admin 3 files/12 tests, customer 2 files/4 tests
- `npm run test:e2e` 통과: admin 3 viewport, customer 3 viewport
- `npm run build` 통과: 두 앱 Vite production build 생성
- `npm audit --omit=dev` 결과 production dependency 취약점 0건

반응형 증거:

- Admin 390: `admin-menu-manager/test-results/admin-responsive-dashboard-shell-smoke-at-compact-chromium/admin-dashboard-compact.png`
- Admin 768: `admin-menu-manager/test-results/admin-responsive-dashboard-shell-smoke-at-medium-chromium/admin-dashboard-medium.png`
- Admin 1440: `admin-menu-manager/test-results/admin-responsive-dashboard-shell-smoke-at-wide-chromium/admin-dashboard-wide.png`
- Customer 390: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-compact-chromium/customer-menu-compact.png`
- Customer 768: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-medium-chromium/customer-menu-medium.png`
- Customer 1440: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-wide-chromium/customer-menu-wide.png`

## 2026-06-23 — D01 최초 관리자·로그인·로그아웃·비밀번호 변경

- `users`, `sessions`, normalized username unique index, session token unique index migration 추가
- `setup/recovery/login/logout/session/change-password` contract와 password policy schema 추가
- PBKDF2 password hash abstraction 추가. 테스트/dev fixture만 fast hasher 사용
- 세션 토큰 원문은 저장하지 않고 SHA-256 hash로 저장
- setup 1회 제한, recovery token 검증, 5회 실패/15분 잠금, 수동 unlock 기반, inactive session revoke 기반 구현
- sliding 8시간 session과 5분 touch throttling 구현
- CSRF double-submit 기반 mutation 검증 구현
- forced password-change 사용자는 change-password/logout 외 보호 기능 차단
- `/setup`, `/login`, `/change-password`, `/recovery` Auth UI 구현
- D02 dashboard는 인증된 placeholder까지만 유지하고 운영 위젯은 구현하지 않음
- 로컬 dev server API는 실제 Hono auth app을 사용하며 production에 포함되지 않는 `/__dev/reset-auth`만 별도 제공

검증:

- `npm run verify:migrations`: 0000, 0001 migration을 빈 SQLite DB에 적용 성공
- `npm run lint`: 통과
- `npm run typecheck`: 통과
- `npm run test`: admin 4 files/18 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 9 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Auth screens 390: `admin-menu-manager/test-results/auth-flow-D01-auth-screens-a2e2e-d-preserve-input-at-compact-chromium/auth-screens-compact.png`
- Auth screens 768: `admin-menu-manager/test-results/auth-flow-D01-auth-screens-84faa-nd-preserve-input-at-medium-chromium/auth-screens-medium.png`
- Auth screens 1440: `admin-menu-manager/test-results/auth-flow-D01-auth-screens-render-and-preserve-input-at-wide-chromium/auth-screens-wide.png`
- Forced auth flow 390: `admin-menu-manager/test-results/auth-flow-D01-forced-password-flow-at-compact-chromium/auth-flow-compact.png`
- Forced auth flow 768: `admin-menu-manager/test-results/auth-flow-D01-forced-password-flow-at-medium-chromium/auth-flow-medium.png`
- Forced auth flow 1440: `admin-menu-manager/test-results/auth-flow-D01-forced-password-flow-at-wide-chromium/auth-flow-wide.png`

## 2026-06-23 — D02 대시보드

- `/api/dashboard` actor별 summary contract와 API route 추가
- system-admin과 bar-user의 dashboard mode, metric, quick action, activity 범위 분리 확인
- D03 바 schema를 선행 구현하지 않고 `accessibleBars: []`와 명시적 unavailable 위젯으로 처리
- 사용자 상태 summary는 auth repository에서 active/inactive/locked 집계로 제공
- `/dashboard` UI를 API 응답에 연결하고 loading, unauthenticated, forbidden, error, empty, unavailable 상태 구현
- 같은 `/dashboard` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인
- dev-only `/__dev/reset-auth` fixture에 `admin1`, `staff1`, `forced1` 추가. production secret 없음
- D01 forced-password 후 dashboard logout 흐름 유지 확인

검증:

- `npm run verify:migrations`: 0000, 0001 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 5 files/22 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 15 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- System admin 390: `admin-menu-manager/test-results/dashboard-D02-system-admin-dashboard-at-compact-chromium/dashboard-admin-compact.png`
- System admin 768: `admin-menu-manager/test-results/dashboard-D02-system-admin-dashboard-at-medium-chromium/dashboard-admin-medium.png`
- System admin 1440: `admin-menu-manager/test-results/dashboard-D02-system-admin-dashboard-at-wide-chromium/dashboard-admin-wide.png`
- Bar user 390: `admin-menu-manager/test-results/dashboard-D02-bar-user-dashboard-at-compact-chromium/dashboard-staff-compact.png`
- Bar user 768: `admin-menu-manager/test-results/dashboard-D02-bar-user-dashboard-at-medium-chromium/dashboard-staff-medium.png`
- Bar user 1440: `admin-menu-manager/test-results/dashboard-D02-bar-user-dashboard-at-wide-chromium/dashboard-staff-wide.png`

resize 상태 보존:

- D02 E2E에서 390↔1440 resize 후 `/dashboard` URL 유지 확인
- D03 전 바 목록이 비어 있으므로 `선택된 바 없음` empty selection 상태가 resize 후 유지됨을 확인

## 2026-06-23 — D03 바 등록·목록·상세

- `bars`, `bar_public_counters` schema와 `0002_d03_bars.sql` migration 추가
- 바 생성/list/detail contract와 Zod request/response 검증 추가
- system-admin 전용 권한 guard, 401/403/404/409 오류 경로 구현
- 서버 생성 slug와 padding 없는 Base64URL `encodedSlug` 생성, slug 충돌 retry와 terminal 409 처리 확인
- 생성 직후 `active` 바와 `preparing` 고객 메뉴판 상태, public counter 초기값을 함께 생성
- `/api/bars`, `/api/bars/:barId` route 추가. mutation은 기존 session cookie와 CSRF header를 사용
- D02 dashboard summary가 실제 바 repository의 active/total 집계를 사용하도록 연결
- `/bars`, `/bars/new`, `/bars/{barId}` UI를 API에 연결하고 loading, empty, error, unauthenticated, forbidden 상태 구현
- 같은 URL에서 compact card/list, medium/wide table/form/detail layout만 적응하며 device route/redirect 없음 확인
- D15 전 실제 GitHub/Cloudflare 쓰기는 수행하지 않고 preparing 상태만 표시

검증:

- `npm run verify:migrations`: 0000, 0001, 0002 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 6 files/28 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 19 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Bars list 390: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-compact-chromium/bars-list-compact.png`
- Bars list 768: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-medium-chromium/bars-list-medium.png`
- Bars list 1440: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-wide-chromium/bars-list-wide.png`
- New bar 390: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-compact-chromium/bar-new-compact.png`
- New bar 768: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-medium-chromium/bar-new-medium.png`
- New bar 1440: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-wide-chromium/bar-new-wide.png`
- Bar detail 390: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-compact-chromium/bar-detail-compact.png`
- Bar detail 768: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-medium-chromium/bar-detail-medium.png`
- Bar detail 1440: `admin-menu-manager/test-results/bars-D03-bar-create-list-detail-at-wide-chromium/bar-detail-wide.png`

resize 상태 보존:

- D03 E2E에서 `/bars/new` 작성 중 390↔1440 resize 후 바 이름과 통화 입력값 유지 확인
- `/bars` 검색어, 상태 필터, 선택 바 요약이 resize 후 유지됨을 확인
- `/bars/{barId}` 상세에서 같은 URL 유지와 compact sticky action 접근성을 확인

## 2026-06-23 — D04 사용자 계정 관리

- `/api/system/users` 목록·생성·상세와 activate/deactivate/unlock/reset-password command 추가
- 사용자 검색·상태 필터·페이지네이션 contract와 임시 비밀번호 1회 반환 contract 추가
- system-admin guard 적용. forced password 사용자와 일반 사용자는 사용자 관리 API 접근 차단
- 임시 비밀번호는 생성/초기화 응답에만 포함하고 목록·상세 응답에는 미포함 확인
- username 정규화 중복을 409 `USERNAME_ALREADY_EXISTS`로 반환
- 비활성화 시 기존 active session을 즉시 revoke하고 기존 쿠키가 `SESSION_EXPIRED`가 됨을 확인
- 비밀번호 초기화 시 새 임시 비밀번호와 forced password change를 설정하고 기존 세션은 기능 접근에서 `PASSWORD_CHANGE_REQUIRED`로 차단
- `/system/users` UI를 API에 연결하고 loading, empty/search empty, error, unauthenticated, forbidden, temporary password notice, destructive confirmation 상태 구현
- 같은 `/system/users` route에서 wide table, compact card와 full-screen dialog로만 적응하며 device route/redirect 없음 확인
- D05 전 바 소속은 추측 생성하지 않고 `D05 이후 바 소속 배정`으로 표시

검증:

- `npm run verify:migrations`: 0000, 0001, 0002 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 7 files/39 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 23 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- User create 390: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-compact-chromium/user-create-compact.png`
- User create 768: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-medium-chromium/user-create-medium.png`
- User create 1440: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-wide-chromium/user-create-wide.png`
- Users list 390: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-compact-chromium/users-list-compact.png`
- Users list 768: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-medium-chromium/users-list-medium.png`
- Users list 1440: `admin-menu-manager/test-results/system-users-D04-user-create-deactivate-at-wide-chromium/users-list-wide.png`

resize 상태 보존:

- D04 E2E에서 `/system/users` 생성 dialog 작성 중 390↔1440 resize 후 아이디 입력값 유지 확인
- `/system/users` 검색어, 상태 필터, 선택 사용자 요약이 resize 후 유지됨을 확인
- compact full-screen dialog와 action card가 동일 API mutation을 사용함을 확인

## 2026-06-23 — D05 바별 회원·역할·권한

- `bar_memberships`, `bar_role_permissions` schema와 `0003_d05_memberships.sql` migration 추가
- 기존 바와 신규 바 모두 owner/manager/staff 기본 권한 seed 확인
- owner/manager는 모든 권한 허용, staff는 주문 운영만 기본 허용하는 contract와 repository 기본값 추가
- `/api/bars/:barId/members` 목록·추가, `/api/bars/:barId/members/:membershipId` 역할 변경, deactivate command 추가
- `/api/bars/:barId/role-permissions` 조회·저장과 `/api/bars/:barId/current-permissions` active membership guard 추가
- system-admin 회원 추가 금지, inactive user 차단, duplicate active membership 409, tenant 범위 밖 대상 404 처리 확인
- D03 바 상세 조회는 D05 이후 non-member에게 바 존재를 누설하지 않도록 404로 조정
- dashboard와 bar detail은 system-admin 또는 active membership이 있는 사용자만 접근 가능하도록 연결
- `/bars/{barId}/members` UI를 WF-10 기준으로 구현하고 wide table+side editor, compact card+full-screen editor, role permission accordion을 같은 state/API에 연결
- 같은 `/bars/{barId}/members` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000, 0001, 0002, 0003 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 8 files/48 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 27 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Member add 390: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-compact-chromium/member-add-compact.png`
- Member add 768: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-medium-chromium/member-add-medium.png`
- Member add 1440: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-wide-chromium/member-add-wide.png`
- Members list 390: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-compact-chromium/members-list-compact.png`
- Members list 768: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-medium-chromium/members-list-medium.png`
- Members list 1440: `admin-menu-manager/test-results/memberships-D05-member-add-role-permissions-at-wide-chromium/members-list-wide.png`

resize 상태 보존:

- D05 E2E에서 회원 추가 dialog 작성 중 390 resize 후 `/bars/{barId}/members` URL, 사용자 검색어, 선택 사용자, 추가 역할 유지 확인
- D05 E2E에서 역할 권한 checkbox draft가 resize 후 유지되고 같은 `PATCH /role-permissions` mutation으로 저장됨을 확인
- Vitest responsive shell에서 `/bars/bar-1/members` 선택 회원과 `staff 메뉴 편집` checkbox draft가 1440→390 resize 후 유지됨을 확인

## 2026-06-23 — D06 바 기본 정보·영업시간·링크·통화

- `bars`에 공개 profile 필드와 `settings_draft_hash`를 추가하고 `bar_business_hours`, `bar_links` schema와 `0004_d06_bar_settings.sql` migration 추가
- 바 설정 contract에서 소개 500자, 국내 전화번호 숫자, http/https 지도·링크 URL, 링크 최대 5개, 요일별 영업시간 겹침 검증 확인
- 익일 마감 구간이 다음 요일 구간과 겹치면 400 `INPUT_INVALID`로 차단 확인
- `/api/bars/:barId/settings` 조회·저장 API 추가. 저장 시 canonical draft hash 갱신 확인
- settings read는 system-admin 또는 active membership, settings mutation은 system-admin 또는 `canEditMenu` 권한으로 제한 확인
- 통화 변경은 system-admin만 가능하며 일반 manager는 403 `CURRENCY_SYSTEM_ADMIN_REQUIRED`로 차단 확인
- 다른 바 또는 비소속 바 settings 접근은 404 `BAR_NOT_FOUND`로 숨김 확인
- `/bars/{barId}/settings` UI를 WF-11 기준으로 구현하고 profile, 영업시간, 외부 링크, 운영 설정 section을 같은 form state와 mutation에 연결
- 바 개요 `/bars/{barId}`에서 D06 바 설정 진입점 활성화
- 같은 `/bars/{barId}/settings` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000, 0001, 0002, 0003, 0004 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 9 files/56 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 31 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Settings form 390: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-compact-chromium/bar-settings-form-compact.png`
- Settings form 768: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-medium-chromium/bar-settings-form-medium.png`
- Settings form 1440: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-wide-chromium/bar-settings-form-wide.png`
- Settings saved 390: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-compact-chromium/bar-settings-saved-compact.png`
- Settings saved 768: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-medium-chromium/bar-settings-saved-medium.png`
- Settings saved 1440: `admin-menu-manager/test-results/bar-settings-D06-bar-settings-save-at-wide-chromium/bar-settings-saved-wide.png`

resize 상태 보존:

- D06 E2E에서 바 이름, 월요일 영업시간, 링크 reorder 상태가 390 resize 후 `/bars/{barId}/settings` URL과 함께 유지됨을 확인
- Vitest responsive shell에서 `/bars/bar-1/settings` 바 이름과 링크 URL 미저장 입력이 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 저장 성공 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D07 품목 유형·고정 템플릿·포도 품종

- `system_item_types`, `bar_item_types`, `bar_item_type_overrides`, `grape_varieties`, `grape_variety_candidates` schema와 `0005_d07_item_types.sql` migration 추가
- fixed template union(`general`, `wine`, `whisky`, `spirit`, `beer`, `cocktail`, `food`, `cigar`)과 기본 price label contract 추가
- system common item type CRUD, bar-specific item type CRUD, bar별 common type hide/price label override API 추가
- in-use item type 비활성화/삭제는 409 `ITEM_TYPE_IN_USE`로 차단 확인
- normalized duplicate item type name은 409 `ITEM_TYPE_NAME_EXISTS`로 차단 확인
- 포도 품종 후보 제출·승인·반려 API 추가. 승인 전 후보는 승인 품종 목록에 노출되지 않음 확인
- 포도 품종 승인 중복은 409 `GRAPE_VARIETY_ALREADY_APPROVED`, non-pending 후보 재처리는 409 `GRAPE_CANDIDATE_NOT_PENDING`으로 차단 확인
- `/system/item-types` UI를 WF-12 기준으로 구현하고 시스템 공통, 바 전용, 포도 품종 후보를 같은 route 내부 탭으로 연결
- dev fixture reset이 D07 in-memory item type repository까지 초기화하도록 연결
- 같은 `/system/item-types` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000~0005 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 10 files/64 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 34 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Common type form 390: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-compact-chromium/item-type-common-form-compact.png`
- Common type form 768: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-medium-chromium/item-type-common-form-medium.png`
- Common type form 1440: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-wide-chromium/item-type-common-form-wide.png`
- Completed flow 390: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-compact-chromium/item-types-complete-compact.png`
- Completed flow 768: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-medium-chromium/item-types-complete-medium.png`
- Completed flow 1440: `admin-menu-manager/test-results/item-types-D07-item-type-create-override-approve-at-wide-chromium/item-types-complete-wide.png`

resize 상태 보존:

- D07 E2E에서 공통 유형 생성 중 390 resize 후 `/system/item-types` URL, 유형 이름, 기본 가격 label draft 유지 확인
- Vitest responsive shell에서 `/system/item-types` 공통 유형 검색어, 선택 유형, 기본 가격 label draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 공통 유형 생성, 바별 common override 저장, 바 전용 유형 생성, 포도 후보 제출·승인 성공 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D08 배지·색상

- `badge_colors`, `system_badges`, `bar_badge_visibility`, `bar_badges`, `menu_item_badges` schema와 `0006_d08_badges.sql` migration 추가
- 공통 색상은 `#RRGGBB`/`#RRGGBBAA`만 허용하고 contract의 대비 계산으로 `#000000` 또는 `#FFFFFF` 텍스트 색상을 산출함 확인
- system-admin 전용 색상 CRUD와 공통 배지 create/update API 추가
- 사용 중인 색상 비활성화는 409 `BADGE_COLOR_REPLACEMENT_REQUIRED`로 대체 색상 선택을 요구하고, 확인 후 시스템/바 배지 색상 참조를 대체함 확인
- 공통 배지 비활성화는 사용 중일 때 409 `BADGE_IN_USE_CONFIRM_REQUIRED`와 `details.usageCount`를 반환하고, 확인 후 메뉴 배지 assignment를 제거함 확인
- 바별 공통 배지 표시 상태는 기본 숨김이며 `/api/bars/:barId/badges/visibility/:systemBadgeId`로 저장함 확인
- 바 전용 배지 create/update/delete API 추가. system-admin 또는 해당 바 `canEditMenu` active member만 허용하고, 다른 바/비소속 바는 404로 숨김 확인
- 바 전용 배지 삭제·비활성화는 사용 중일 때 impact 확인을 요구하고 확인 후 assignment 제거 또는 삭제함 확인
- `/system/badges` UI를 WF-13 기준으로 구현하고 배지 tab, 허용 색상 tab, 바 선택, HEX/color picker sync, 대비 preview, 영향 확인 checkbox를 같은 route 내부 state로 연결
- dev fixture reset이 D08 in-memory badge repository까지 초기화하도록 연결
- 같은 `/system/badges` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000~0006 migration을 빈 SQLite DB에 적용 성공
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 11 files/72 tests, customer 2 files/4 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/badges.spec.ts`: D08 3 viewport 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Color form 390: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-compact-chromium/badge-color-form-compact.png`
- Color form 768: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-medium-chromium/badge-color-form-medium.png`
- Color form 1440: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-wide-chromium/badge-color-form-wide.png`
- Completed flow 390: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-compact-chromium/badges-complete-compact.png`
- Completed flow 768: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-medium-chromium/badges-complete-medium.png`
- Completed flow 1440: `admin-menu-manager/test-results/badges-D08-badge-and-color-management-at-wide-chromium/badges-complete-wide.png`

resize 상태 보존:

- D08 E2E에서 허용 색상 추가 중 390 resize 후 `/system/badges` URL, 선택 tab, HEX draft 유지 확인
- Vitest responsive shell에서 `/system/badges` 공통 배지 검색어, 선택 바, 색상 tab, HEX draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 색상 추가, 공통 배지 추가, 바별 공통 배지 표시 전환, 바 전용 배지 추가 성공 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D09 카테고리 관리

- `categories` schema와 `0007_d09_categories.sql` migration 추가
- 바별 `cat_N` public id counter, parent/self FK, 같은 단계 이름 unique index, parent/sort index 확인
- 카테고리 contract에서 이름 1~30자, 설명 100자, visible/hidden, 설명 공개 flag, 이동·정렬·삭제 request 검증 추가
- `/api/bars/:barId/categories` 조회·생성, `/api/bars/:barId/categories/reorder`, `/api/bars/:barId/categories/:categoryId` 수정·삭제, `/move` API 추가
- system-admin 또는 해당 바 `canEditMenu` active member만 허용하고, 비소속 바와 다른 바 대상은 404로 숨김 확인
- 최대 2단계, 같은 단계 이름 중복 409, parent-with-menu 하위 생성 차단, child 있는 상위의 3단계 이동 차단, sibling set 불일치 정렬 409 확인
- 메뉴가 직접 있는 카테고리 삭제 409, 메뉴 없는 하위 카테고리 cascade 삭제 확인 required 409와 확인 후 삭제 확인
- `/bars/{barId}/categories` UI를 WF-14 기준으로 구현하고 tree/list, 편집 form, drag/drop, up/down 버튼, 상위 선택 fallback, cascade 삭제 확인을 같은 route state와 API에 연결
- 바 개요 `/bars/{barId}`에서 D09 카테고리 관리 진입점 활성화
- dev fixture reset이 D09 in-memory category repository까지 초기화하도록 연결
- 같은 `/bars/{barId}/categories` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000~0007 migration을 빈 SQLite DB에 적용 성공
- `npm run test --workspace admin-menu-manager -- tests/integration/categoriesApi.test.ts tests/e2e/responsiveShell.test.tsx`: 31 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/categories.spec.ts`: D09 3 viewport 통과
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 12 files/79 tests, customer 2 files/4 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Category form 390: `admin-menu-manager/test-results/categories-D09-category-management-at-compact-chromium/category-form-compact.png`
- Category form 768: `admin-menu-manager/test-results/categories-D09-category-management-at-medium-chromium/category-form-medium.png`
- Category form 1440: `admin-menu-manager/test-results/categories-D09-category-management-at-wide-chromium/category-form-wide.png`
- Completed flow 390: `admin-menu-manager/test-results/categories-D09-category-management-at-compact-chromium/categories-complete-compact.png`
- Completed flow 768: `admin-menu-manager/test-results/categories-D09-category-management-at-medium-chromium/categories-complete-medium.png`
- Completed flow 1440: `admin-menu-manager/test-results/categories-D09-category-management-at-wide-chromium/categories-complete-wide.png`

resize 상태 보존:

- D09 E2E에서 `/bars/{barId}/categories` 작성 중 390 resize 후 URL과 카테고리 이름 draft 유지 확인
- Vitest responsive shell에서 `/bars/bar-1/categories` 검색어, 선택 카테고리, 카테고리 이름 draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 상위/하위 생성, 저장, 정렬 버튼 fallback, 상위 선택 fallback 이동, cascade 삭제 확인 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D10 메뉴 기본 CRUD

- `menu_items` schema와 `0008_d10_menu_items.sql` migration 추가
- 바별 `menu_N` public id counter, category FK, system/bar item type FK, normalized menu name unique index, category/sort index 확인
- 메뉴 contract에서 이름 1~50자, 설명 200자, `available`/`sold_out`, visible/hidden, ABV 0~100 및 소수점 둘째 자리 제한, optional item type 선택 검증 추가
- `/api/bars/:barId/menu-items` 조회·생성, `/api/bars/:barId/menu-items/:menuItemId` 조회·수정·삭제 API 추가
- system-admin 또는 active membership read 허용, mutation은 `canEditMenu` 권한으로 제한. 비소속 바와 다른 바 메뉴 ID는 404로 숨김 확인
- leaf category가 아닌 대상 생성/이동은 409 `MENU_CATEGORY_NOT_LEAF`로 차단 확인
- 같은 바 normalized menu name 중복은 409 `MENU_NAME_EXISTS`로 차단 확인
- 메뉴가 직접 들어간 카테고리는 D09 child 생성 guard와 삭제 guard에서 실제 `menu_items` count로 차단됨 확인
- 메뉴가 품목 유형을 참조하면 D07 item type 비활성화가 409 `ITEM_TYPE_IN_USE`로 차단됨 확인
- 카테고리 변경 시 대상 카테고리 최상단 `sortOrder: 0` placement와 기존 항목 shift 확인
- D1 delete path에서 `menu_item_badges` 참조를 먼저 정리하고 메뉴를 영구 삭제하도록 구현
- `/bars/{barId}/menus`, `/bars/{barId}/menus/new`, `/bars/{barId}/menus/{menuItemId}` UI를 WF-15/WF-16 기준으로 구현하고 목록 필터, create/edit form, save/revert, delete confirm을 같은 route state와 API에 연결
- 바 개요 `/bars/{barId}`에서 D10 메뉴 관리 진입점 활성화
- dev fixture reset이 D10 in-memory menu item repository까지 초기화하고 category/type usage sink에 연결되도록 구현
- 같은 `/bars/{barId}/menus...` route에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인

검증:

- `npm run verify:migrations`: 0000~0008 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run test --workspace admin-menu-manager -- tests/integration/menuItemsApi.test.ts`: D10 integration 3 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/integration/menuItemsApi.test.ts`: 39 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/menu-items.spec.ts`: D10 3 viewport 통과
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 13 files/90 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 43 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- New menu form 390: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-compact-chromium/menu-new-compact.png`
- New menu form 768: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-medium-chromium/menu-new-medium.png`
- New menu form 1440: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-wide-chromium/menu-new-wide.png`
- Menus list 390: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-compact-chromium/menus-list-compact.png`
- Menus list 768: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-medium-chromium/menus-list-medium.png`
- Menus list 1440: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-wide-chromium/menus-list-wide.png`
- Completed flow 390: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-compact-chromium/menus-complete-compact.png`
- Completed flow 768: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-medium-chromium/menus-complete-medium.png`
- Completed flow 1440: `admin-menu-manager/test-results/menu-items-D10-menu-CRUD-at-wide-chromium/menus-complete-wide.png`

resize 상태 보존:

- D10 E2E에서 `/bars/{barId}/menus/new` 작성 중 390 resize 후 URL, 메뉴 이름, ABV draft 유지 확인
- D10 E2E에서 `/bars/{barId}/menus/{menuItemId}` 수정 중 390 resize 후 URL, 메뉴 이름, 노출 checkbox draft 유지 확인
- D10 E2E에서 `/bars/{barId}/menus` 목록 검색어가 resize 후 유지됨을 확인
- Vitest responsive shell에서 `/bars/bar-1/menus` 검색어, 판매 상태 필터, 선택 메뉴가 1440→390 resize 후 유지됨을 확인
- Vitest responsive shell에서 `/bars/bar-1/menus/menu-1` 메뉴 이름, ABV, 노출 checkbox draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 create→modify→list→delete 성공 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D11 가격·상세 템플릿·내부 메모

- `0009_d11_menu_item_details.sql` migration 추가: `menu_items.internal_memo`, `menu_item_prices`, `menu_item_details` 생성
- DB에서 가격 label 정규화 중복 unique, 가격/order index, 상세 template enum, JSON 유효성, 내부 메모 길이 제한 확인
- Drizzle schema에 `menuItemPrices`, `menuItemDetails`, `menuItems.internalMemo` 추가
- 메뉴 contract에 가격 0~10개, 정수 `amountMinor`, fixed detail template discriminated union, 내부 메모 DTO와 권한 flag 추가
- memory/D1 menu item repository에 가격 replace, 상세 upsert/delete, 내부 메모 저장 경로 추가
- service에서 active member read, `canEditMenu` 가격·상세 mutation, owner/system-admin 내부 메모 mutation 권한 분리 확인
- manager는 가격·상세를 수정할 수 있지만 내부 메모 변경은 403 `INTERNAL_MEMO_OWNER_REQUIRED`로 차단됨 확인
- 상세 template 변경 시 기존 상세값이 있으면 409 `DETAIL_TEMPLATE_RESET_REQUIRED`와 `{fromTemplate,toTemplate}` details 반환 확인
- 새 메뉴에서 품목 유형 선택 시 기본 가격 label 자동 생성, 가격 행 이동 버튼과 drag/drop, 템플릿별 동적 상세 form, 내부 메모 read/write 상태 구현
- `/bars/{barId}/menus/new`, `/bars/{barId}/menus/{menuItemId}` 같은 URL에서 compact/medium/wide layout만 전환하며 device route/redirect 없음 확인
- dev server `http://127.0.0.1:5173/bars/bar-1/menus/menu-1` 200 응답 확인

검증:

- `npm run verify:migrations`: 0000~0009 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run test --workspace admin-menu-manager -- tests/contracts/menuItemDetails.test.ts tests/integration/menuItemsApi.test.ts`: 7 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/contracts/menuItemDetails.test.ts tests/integration/menuItemsApi.test.ts`: 43 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/menu-items.spec.ts`: D11 3 viewport 통과
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 14 files/94 tests, customer 2 files/4 tests 통과
- `npm run test:e2e`: admin 43 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- New menu form 390: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-compact-chromium/menu-new-compact.png`
- New menu form 768: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-medium-chromium/menu-new-medium.png`
- New menu form 1440: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-wide-chromium/menu-new-wide.png`
- Menus list 390: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-compact-chromium/menus-list-compact.png`
- Menus list 768: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-medium-chromium/menus-list-medium.png`
- Menus list 1440: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-wide-chromium/menus-list-wide.png`
- Completed flow 390: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-compact-chromium/menus-complete-compact.png`
- Completed flow 768: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-medium-chromium/menus-complete-medium.png`
- Completed flow 1440: `admin-menu-manager/test-results/menu-items-D11-menu-price-detail-memo-save-at-wide-chromium/menus-complete-wide.png`

resize 상태 보존:

- D11 E2E에서 `/bars/{barId}/menus/new` 작성 중 390 resize 후 URL, 메뉴 이름, ABV, 가격 금액, 상세 brand, 내부 메모 draft 유지 확인
- D11 E2E에서 `/bars/{barId}/menus/{menuItemId}` 수정 중 390 resize 후 URL, 메뉴 이름, 노출 checkbox, 가격 금액, 상세 brand, 내부 메모 draft 유지 확인
- Vitest responsive shell에서 `/bars/bar-1/menus/menu-1` 메뉴 이름, ABV, 노출 checkbox, 가격 금액, 상세 brand, 내부 메모 draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 가격·상세·메모 create→modify→list→delete 성공 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D12 메뉴 목록·검색·일괄 수정·배지

- `0010_d12_menu_badge_assignments.sql` migration 추가: 기존 `menu_item_badges`를 FK 포함 table로 재작성하고 `bar_id/menu_item_id/display_order` unique index 추가
- 기존 적용 migration을 수정하지 않고 0000~0010 순차 적용 검증
- 메뉴 목록 contract에 검색어, 카테고리, 품목 유형, 판매 상태, 노출, 배지 필터와 `badgeOptions`, item별 `badges` DTO 추가
- bulk mutation contract에 `expectedCount`, 상태/노출/category/sortOrder/badge 변경, 메뉴당 배지 최대 3개와 중복 배지 금지 검증 추가
- `/api/bars/:barId/menu-items` query filter와 `/api/bars/:barId/menu-items/bulk` mutation 추가
- system-admin 또는 active member read 허용, mutation은 `canEditMenu` 권한으로 제한. 미인증 401, 권한 없음 403, 다른 바 대상 404, impact mismatch/비leaf category/사용 불가 배지 409 확인
- memory/D1 repository에 menu badge assignment replace/list, bulk state/category/order/badge update, badge usage sink 연결 추가
- `/bars/{barId}/menus` UI를 WF-15 기준으로 전체 목록/카테고리 보기, wide table, compact card, filter controls, row quick edit, order up/down, badge selection/reorder, sticky bulk draft panel로 구현
- compact/medium/wide 모두 같은 URL과 filter/selection/draft/mutation state를 공유하고 device route/redirect 없음 확인
- `sold_out` 메뉴는 admin 목록에서 배지 assignment를 볼 수 있지만 공개 JSON에서는 숨겨야 함을 UI와 테스트 기반으로 표시. 실제 public DTO 출력은 D13 범위로 남김

검증:

- `npm run verify:migrations`: 0000~0010 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 14 files/97 tests, customer 2 files/4 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/menu-items.spec.ts`: D11+D12 menu flows 6 tests 통과
- `npm run test:e2e`: admin 46 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Bulk draft 390: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-05100-and-resize-state-at-compact-chromium/d12-bulk-draft-compact.png`
- Bulk draft 768: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-e7d7f--and-resize-state-at-medium-chromium/d12-bulk-draft-medium.png`
- Bulk draft 1440: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-64d64-es-and-resize-state-at-wide-chromium/d12-bulk-draft-wide.png`
- Bulk saved 390: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-05100-and-resize-state-at-compact-chromium/d12-bulk-saved-compact.png`
- Bulk saved 768: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-e7d7f--and-resize-state-at-medium-chromium/d12-bulk-saved-medium.png`
- Bulk saved 1440: `admin-menu-manager/test-results/menu-items-D12-menu-list-b-64d64-es-and-resize-state-at-wide-chromium/d12-bulk-saved-wide.png`

resize 상태 보존:

- D12 E2E에서 `/bars/{barId}/menus` 선택 2개, 일괄 품절·숨김·카테고리 이동·추천 배지 draft가 390 resize 후 URL, 선택 checkbox, 미저장 변경 수를 유지함을 확인
- Vitest responsive shell에서 `/bars/bar-1/menus` 검색어, 품목 유형 필터, 배지 필터, 선택 checkbox, quick sale draft, bulk category draft가 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 bulk draft 적용, 최종 저장, sold_out/hidden/badge filter, 카테고리 보기 전환 후 불필요한 가로 스크롤과 44px 미만 touch target 없음 확인

## 2026-06-23 — D13 Public DTO·JSON Schema·미리보기

- 공유 `PublicMenu` contract와 Zod schema, forbidden public key scan, canonical content hash 계산 추가
- 관리자와 고객 앱이 같은 public parser, filter, renderer를 사용하도록 `shared/` contract/renderer로 통합
- `/api/bars/:barId/preview` API 추가. system-admin 또는 active membership만 읽을 수 있고, 비소속/다른 바 대상은 404로 숨김 확인
- 저장된 bar settings, visible category tree, visible menu item, price, detail, badge 데이터를 public DTO로 변환
- 내부 DB ID, 사용자 정보, 내부 메모, token/secret 계열 field가 public JSON에 포함되지 않음을 테스트로 확인
- hidden 카테고리·메뉴 제외, hidden parent 하위 제외, visible empty category 유지, sold_out 메뉴 가격·용량·배지 숨김 확인
- content hash는 public content만 대상으로 계산하고 `revision`, `publishedAt`, `generatedAt`, `contentHash` 변동에는 흔들리지 않도록 분리
- `/bars/{barId}/preview` UI를 WF-17 기준으로 구현하고 schema 상태, hash, 범위 선택, 고객 메뉴 검색, 실제 고객 renderer를 같은 route에 연결
- 관리자 header에 현재 작업 바 selector 추가. 바 scope route에서 바 변경 시 같은 route pattern의 barId만 교체하고 viewport 기반 redirect는 사용하지 않음
- sidebar는 actor mode와 선택 바 권한에 따라 system-admin, owner/manager, staff가 접근 가능한 메뉴만 노출하도록 필터링
- local dev/test full fixture에 Sample Bar, 카테고리, 메뉴, 가격, 상세, sold_out, hidden, 배지, owner/manager/staff 계정을 추가. production secret 없음
- 고객 fixture JSON을 D13 public DTO shape로 갱신하고 고객 `/{encodedSlug}` 단일 route smoke를 유지

검증:

- `npm run verify:migrations`: 0000~0010 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 15 files/104 tests, customer 2 files/4 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/preview.spec.ts`: D13 preview 390/768/1440 + staff sidebar 4 tests 통과
- `npm run test:e2e`: admin 50 tests, customer 3 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Preview 390: `admin-menu-manager/test-results/preview-D13-public-preview-at-compact-chromium/preview-compact.png`
- Preview 768: `admin-menu-manager/test-results/preview-D13-public-preview-at-medium-chromium/preview-medium.png`
- Preview 1440: `admin-menu-manager/test-results/preview-D13-public-preview-at-wide-chromium/preview-wide.png`
- Customer 390: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-compact-chromium/customer-menu-compact.png`
- Customer 768: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-medium-chromium/customer-menu-medium.png`
- Customer 1440: `customer-menu-board/test-results/customer-responsive-customer-menu-smoke-at-wide-chromium/customer-menu-wide.png`

resize 상태 보존:

- D13 E2E에서 `/bars/{barId}/preview` 범위 선택, 고객 메뉴 검색어, 현재 작업 바 선택이 390 resize 후 같은 URL에서 유지됨을 확인
- D13 E2E에서 세 viewport 모두 schema valid, sold_out 가격 숨김, 내부 메모 비노출, 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인
- D13 staff E2E에서 staff는 미리보기·주문만 접근 메뉴로 보고 바 회원, 사용자 계정, 카테고리, 메뉴 편집 메뉴가 sidebar에 노출되지 않음을 확인
- Vitest responsive shell에서 header 바 선택 후 같은 `/bars/{barId}/preview` route의 선택 바와 검색 상태가 resize 후 유지됨을 확인

## 2026-06-24 — D13 관리자 셸 바 선택·권한 sidebar 회귀 보강

- 관리자 header selector의 visible label을 `현재 작업 바`로 명확히 유지하고, 선택 바 변경 시 이전 바의 권한 응답을 즉시 비워 stale permission이 다른 바 sidebar에 섞이지 않도록 보강
- 권한 응답이 아직 도착하지 않은 첫 렌더에서는 역할 기본 권한을 sidebar 계산에 사용하고, API 응답이 도착하면 `barId`가 현재 선택 바와 일치하는 경우에만 실제 권한으로 교체
- local full fixture에 `Whisky Lab` 두 번째 바를 추가해 `Sample Bar`와 함께 selector 전환, manager/owner 카테고리·메뉴 접근, staff 제한 메뉴 숨김을 화면에서 확인할 수 있게 함
- `tests/e2e/preview.spec.ts`에 selector option 2개 확인, staff 제한 메뉴 hidden, manager의 `Whisky Lab` 선택 후 `/bars/{barId}/categories` 이동 인수 테스트 추가

검증:

- `npm run typecheck --workspace admin-menu-manager`: 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx`: 45 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/preview.spec.ts`: 5 tests 통과
- `npm run lint`: 통과
- `npm run verify:migrations`: 18 migrations 빈 SQLite 적용 통과
- `npm run verify:no-device-routes`: 통과
- `npm run typecheck`: admin/customer 통과
- `npm run test`: admin 17 files/131 tests, customer 3 files/13 tests 통과
- `npm run build`: admin/customer 통과. admin chunk size warning은 non-failing
- `npm run test:e2e`: admin 64 tests, customer 4 tests 통과
- 실제 브라우저 점검: `admin1`, `owner1`, `manager1`, `staff1` 모두 header `현재 작업 바` selector에서 `Sample Bar`, `Whisky Lab` 두 옵션 확인. admin은 전체 메뉴, owner/manager는 카테고리·메뉴·주문·품목·배지, staff는 미리보기·주문만 노출됨을 확인
- 390/768/1440 manager 화면 증거: `/private/tmp/thebar-manager-shell-compact.png`, `/private/tmp/thebar-manager-shell-medium.png`, `/private/tmp/thebar-manager-shell-wide.png`

## 2026-06-23 — D14 고객 메뉴판 반응형 UI

- 고객 public JSON fetch 정책을 `GET /menus/{encodedSlug}.json`, `cache: no-cache`, `credentials: omit`로 고정
- `/api` 기반 JSON source를 차단하고, React dev StrictMode의 중복 effect에서도 같은 slug의 in-flight JSON 요청을 1회로 합침
- fetch/parser 오류를 `not found`, `schema incompatible`, `schema invalid`, `network/load error`로 구분
- public JSON fixture를 published 상태의 Sample Bar 데이터로 확장: 바 정보, 영업시간, 링크, 추천/위스키/칵테일/푸드/빈 카테고리, 상세 field, sold_out 포함
- `/{encodedSlug}` 고객 UI에 바 정보 접기/펼치기, 검색, 카테고리 탐색, 메뉴 상세 펼침, 가격·ABV·배지·템플릿 상세 표시 구현
- sold_out 메뉴는 이름·설명·ABV와 품절 상태만 보여주고 가격·용량·배지를 숨김
- 5분 idle reset은 검색어, 선택 카테고리, 펼친 메뉴, 바 정보 펼침 상태만 초기화하고 URL은 유지
- shared `PublicMenuRenderer`를 상세 펼침과 hero 표시 옵션으로 확장해 D13 preview와 D14 고객 앱이 같은 public renderer를 계속 사용
- 고객 UI에는 관리자, 로그인, 주문, 결제 링크를 노출하지 않음
- WF-22 원본을 `file:///.../wireframes/index.html#screen=customer-menu&viewport=compact|medium|wide`로 열어 screenshot 생성 확인: `/tmp/thebar-wf22-compact.png`, `/tmp/thebar-wf22-medium.png`, `/tmp/thebar-wf22-wide.png`

검증:

- `npm run verify:migrations`: 0000~0010 migration을 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 15 files/104 tests, customer 3 files/13 tests 통과
- `npm run test:e2e --workspace customer-menu-board`: D14 customer 4 tests 통과
- `npm run test:e2e`: admin 50 tests, customer 4 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건

반응형 증거:

- Customer 390: `customer-menu-board/test-results/customer-responsive-D14-customer-menu-at-compact-chromium/customer-menu-compact.png`
- Customer 768: `customer-menu-board/test-results/customer-responsive-D14-customer-menu-at-medium-chromium/customer-menu-medium.png`
- Customer 1440: `customer-menu-board/test-results/customer-responsive-D14-customer-menu-at-wide-chromium/customer-menu-wide.png`

resize 상태 보존:

- D14 E2E에서 `/{encodedSlug}` URL이 390 resize 후 유지됨을 확인
- 검색어 `맥`, 펼친 `맥캘란 12` 상세 field, 카테고리/전체 보기 state가 resize 후 유지됨을 확인
- 세 viewport 모두 바 정보, 검색, category rail/chips, 품절, 빈 카테고리, 44px touch target, 불필요한 가로 스크롤 없음 확인
- D14 E2E에서 고객 앱 요청 목록에 `/api` 또는 관리자 dev server `127.0.0.1:5173` 호출이 없고 `/menus/YmFyLWE3azJtOQ.json` 요청이 1회임을 확인

## 2026-06-23 — D15 GitHub 발행

- `0011_d15_publications.sql` migration 추가: `publications`, `publication_snapshots`, `publication_locks`, `repository_commit_lock` table과 bar/status/snapshot 조회 index 추가
- publication status/operation/request/response contract 추가. 상태는 `pending`, `building_json`, `validating_json`, `committing_github`, `success`, `failed`, `timeout_unknown`만 허용
- `/api/bars/:barId/publications` GET/POST 추가. POST는 CSRF와 `{ confirmSavedOnly: true }`를 요구하고, system-admin 또는 `directPublishEnabled` owner만 실행 가능
- 현재 저장 메뉴를 D13 public DTO builder로 다시 생성하고 Zod schema를 검증한 뒤 fake GitHub adapter에 기록
- 최초·변경 발행은 `public/menus/{encodedSlug}.json`, 동일 내용 재발행은 `public/publish-triggers/{encodedSlug}.json`을 쓰며 동일 내용 재발행은 기존 revision/publishedAt/contentHash를 유지
- 바별 발행 lock과 고객 repo 전역 commit lock을 분리. 서로 다른 바의 commit도 repo lock으로 직렬화됨을 통합 테스트로 확인
- schema 검증 실패는 `PUBLIC_SCHEMA_INVALID` 422로 반환하고 GitHub commit을 남기지 않음
- fake GitHub create/update는 SHA를 유지하고 commit log를 남기지만 실제 GitHub token, remote push, production deploy는 수행하지 않음
- `/bars/{barId}/publications` UI를 WF-18 기준으로 구현하고 preview/menu 화면의 발행 버튼을 같은 route로 연결
- 발행 확인 panel/sheet, 진행 단계, duplicate click block, 최근 발행 이력, 스냅샷 상세를 compact·medium·wide에서 같은 query/form/selection state로 공유

검증:

- `npm run verify:migrations`: 0000~0011, 총 12 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 16 files/114 tests, customer 3 files/13 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/publications.spec.ts`: D15 publication 390/768/1440 + menu action 4 tests 통과
- `npm run test:e2e`: admin 54 tests, customer 4 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건
- 로컬 서버 확인: `http://127.0.0.1:5173/dashboard` 200 응답

반응형 증거:

- Publication 390: `admin-menu-manager/test-results/publications-D15-GitHub-publication-at-compact-chromium/publication-compact.png`
- Publication 768: `admin-menu-manager/test-results/publications-D15-GitHub-publication-at-medium-chromium/publication-medium.png`
- Publication 1440: `admin-menu-manager/test-results/publications-D15-GitHub-publication-at-wide-chromium/publication-wide.png`

resize 상태 보존:

- D15 E2E에서 `/bars/{barId}/publications` 발행 확인 checkbox가 390 resize 후 같은 URL에서 유지됨을 확인
- Vitest responsive shell에서 발행 확인 상태와 선택한 발행 이력이 1440→390 resize 후 유지됨을 확인
- D15 E2E에서 세 viewport 모두 preview 발행 버튼 진입, 발행 중 버튼 비활성화, fake commit 성공, 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인
- 메뉴 목록의 발행 액션도 `/bars/{barId}/publications` 같은 route로 이동하며 기기별 URL이나 redirect를 만들지 않음을 확인

## 2026-06-23 — D16 Cloudflare 배포 상태·발행 이력

- `0012_d16_publication_deployments.sql` migration 추가: 기존 적용 migration을 수정하지 않고 `publications` status check에 `waiting_cloudflare` 추가, deployment metadata column과 `publications_bar_deployment_idx` 추가
- publication contract에 `waiting_cloudflare`, Cloudflare deployment payload, polling metadata, 최신 공개본과 현재 저장본 content hash 차이 DTO 추가
- fake Cloudflare adapter 추가. GitHub commit 관찰 후 deployment sequence를 반환하며, service는 deployment `sourceCommitSha`가 publication `commitSha`와 일치할 때만 성공으로 처리
- 발행 service는 GitHub commit 후 repository/bar lock을 해제하고 `waiting_cloudflare`로 전환한 뒤 Cloudflare 상태를 조회
- 30초 polling용 GET `/api/bars/:barId/publications`가 대기 중 deployment를 재조정하고 `polling.active`, `intervalMs: 30000`, `timeoutSeconds: 180`을 반환
- 3분 초과 시 `timeout_unknown`과 `CLOUDFLARE_TIMEOUT_UNKNOWN`을 기록하며 success/failed로 거짓 표시하지 않음
- Cloudflare read 실패와 deployment failed는 `failed`로 기록하고 외부 오류 원문을 노출하지 않음
- 성공 publication/snapshot 최근 100건, failed/timeout_unknown 최근 100건 보관 로직 추가
- `/bars/{barId}/publications` 화면을 WF-18 기준으로 D16 이력/배포 중심으로 확장: deployment status, 최신 공개본과 편집본 차이, 30초 polling 표시, 발행 상세 dialog/sheet, timeout_unknown 설명
- medium 이하에서는 발행 이력을 card timeline으로 전환해 가로 overflow를 방지하고, compact·medium·wide 모두 같은 route/query/selection state를 유지

검증:

- `npm run verify:migrations`: 0000~0012, 총 13 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 16 files/118 tests, customer 3 files/13 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/publicationsApi.test.ts`: D16 publication API 10 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- tests/e2e/publications.spec.ts`: D16 publication 390/768/1440 + menu action 4 tests 통과
- `npm run test:e2e`: admin 54 tests, customer 4 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건
- 로컬 서버 확인: `http://127.0.0.1:5173/dashboard` 200 응답
- WF-18 원본 확인: `/tmp/thebar-wf18-compact.png`, `/tmp/thebar-wf18-medium.png`, `/tmp/thebar-wf18-wide.png`

반응형 증거:

- Publication 390: `admin-menu-manager/test-results/publications-D16-Cloudflare-publication-history-at-compact-chromium/publication-compact.png`
- Publication 768: `admin-menu-manager/test-results/publications-D16-Cloudflare-publication-history-at-medium-chromium/publication-medium.png`
- Publication 1440: `admin-menu-manager/test-results/publications-D16-Cloudflare-publication-history-at-wide-chromium/publication-wide.png`

resize 상태 보존:

- D16 E2E에서 `/bars/{barId}/publications` 발행 확인 dialog가 390 resize 후 같은 URL에서 유지됨을 확인
- D16 E2E에서 발행 후 상세 dialog/sheet, target commit SHA와 일치하는 fake Cloudflare deployment success, 페이지 reload 후 발행 이력 복구 확인
- Vitest responsive shell에서 발행 확인 상태와 선택한 발행 이력이 1440→390 resize 후 유지됨을 확인
- 세 viewport 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인

## 2026-06-23 — D17 과거 재발행·바 수명주기

- `0013_d17_bar_lifecycle.sql` migration 추가: 기존 적용 migration을 수정하지 않고 `publications.operation` check를 `snapshot_republish`, `delete_menu_json`, `restore_snapshot`, `restore_preparing`까지 확장하고 `bar_lifecycle_events` table/index 추가
- publication contract/API에 과거 성공 snapshot 재발행 request를 추가하고, `/api/bars/:barId/publications/:publicationId/republish` POST는 CSRF와 `{ confirmCurrentEditUnchanged: true }`를 요구함
- bar contract/API에 lifecycle request/response/event를 추가하고, `/api/bars/:barId/lifecycle` POST는 system admin만 active/inactive 전환 가능
- fake GitHub adapter에 `deleteFile`을 추가해 inactive 전환 시 고객 JSON 삭제 commit과 고객 URL 404 상태를 검증 가능하게 함
- snapshot 재발행은 저장된 snapshot JSON만 새 commit으로 쓰며 현재 D1 편집 데이터, 메뉴, 카테고리를 변경하지 않음
- inactive 전환은 성공 deployment 확인 후 bar status를 `inactive`, publicMenuStatus를 `preparing`으로 바꾸고 lifecycle event를 기록함
- active 전환은 마지막 성공 snapshot이 있으면 `restore_snapshot`, 없으면 `restore_preparing`으로 고객 JSON을 복원하고 lifecycle event를 기록함
- `/bars/{barId}/publications` 화면에 성공 이력 재발행 확인/action 추가. `/bars/{barId}` 화면에 lifecycle 설명, 확인 dialog/sheet, 최근 이벤트 표시 추가
- D13 공통 셸 회귀 확인: full fixture 기준 헤더 현재 작업 바 selector와 권한별 sidebar가 admin/staff에서 정상 동작함

검증:

- `npm run verify:migrations`: 0000~0013, 총 14 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test`: admin 16 files/122 tests, customer 3 files/13 tests 통과
- `npx vitest run --config vitest.config.ts tests/integration/publicationsApi.test.ts`: D15~D17 publication/lifecycle API 14 tests 통과
- `npx playwright test --config playwright.config.ts tests/e2e/publications.spec.ts tests/e2e/bars.spec.ts`: D03/D15/D16/D17 11 tests 통과
- `npm run test:e2e`: admin 57 tests 통과
- `npm run build`: 두 앱 production build 통과
- `npm audit --omit=dev`: production dependency 취약점 0건
- 로컬 서버 확인: `http://127.0.0.1:5173/dashboard` 200 응답, full fixture `POST /__dev/reset-auth?fixtures=full` 적용
- WF-18 원본 확인: `/tmp/thebar-d17-wireframe-publications-compact.png`, `/tmp/thebar-d17-wireframe-publications-medium.png`, `/tmp/thebar-d17-wireframe-publications-wide.png`
- WF-08 원본 확인: `/tmp/thebar-d17-wireframe-bar-overview-compact.png`, `/tmp/thebar-d17-wireframe-bar-overview-medium.png`, `/tmp/thebar-d17-wireframe-bar-overview-wide.png`

반응형 증거:

- Publication 390/768/1440: `admin-menu-manager/test-results/publications-D16-Cloudflare-publication-history-at-compact-chromium/publication-compact.png`, `...-medium-chromium/publication-medium.png`, `...-wide-chromium/publication-wide.png`
- Bar lifecycle 390/768/1440: `admin-menu-manager/test-results/bars-D17-bar-lifecycle-at-compact-chromium/bar-lifecycle-compact.png`, `...-medium-chromium/bar-lifecycle-medium.png`, `...-wide-chromium/bar-lifecycle-wide.png`

resize 상태 보존:

- D17 publication E2E에서 `/bars/{barId}/publications` 재발행 확인 panel이 390 resize 후 같은 URL에서 유지되고 `snapshot_republish` 이력이 표시됨을 확인
- D17 bar lifecycle E2E에서 `/bars/{barId}` 비활성화 확인 dialog가 390 resize 후 같은 URL에서 유지되고, 이어서 inactive 404 설명과 preparing 복원 결과가 같은 route에서 표시됨을 확인
- 세 viewport 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인

## 2026-06-23 — D18 주문 탭 생성·목록·상세

- `0014_d18_order_tabs.sql` migration 추가: `order_tab_counters`, `order_tabs`, `order_tab_events` table과 bar별 tab number unique/index, 상태/label 조회 index 추가
- 주문 탭 status/query/create/update/list/detail/event contract 추가. 상태는 `open`, `checkout_requested`, `closed`, `cancelled`만 허용
- `/api/bars/:barId/order-tabs` GET/POST, `/api/bars/:barId/order-tabs/:tabId` GET/PATCH 추가
- 모든 order tab API는 system-admin 또는 active membership이 있는 사용자만 접근 가능하며, mutation은 `can_manage_orders` 권한과 CSRF를 요구함
- 다른 바 또는 권한 없는 대상은 404/403으로 분리해 tenant 범위 내부 ID를 누설하지 않음
- 생성 시 바별 sequential `tabNumber`를 서버에서 부여하고, 수정은 `expectedVersion`으로 stale update 409 `ORDER_TAB_VERSION_CONFLICT`를 반환함
- closed/cancelled 상태 주문 탭은 수정 불가 409로 차단하는 service guard 추가
- `/bars/{barId}/orders`와 `/bars/{barId}/orders/{orderTabId}` UI를 WF-19/WF-20 기준으로 구현하고 목록, 상태 filter, 검색, 생성 form, 상세 편집, 이벤트 timeline을 같은 API/query/form state에 연결
- 헤더 현재 작업 바 selector는 기존 셸에서 유지하고, sidebar는 권한별로 재정렬해 `can_manage_orders`가 있는 사용자에게만 주문 탭 메뉴를 노출함
- full fixture에 `A1`, `Bar 3`, `B2` 주문 탭 seed를 추가해 화면 확인 직후 목록·상세·상태 filter를 볼 수 있게 함

검증:

- `node scripts/verify-sqlite-migrations.mjs`: 0000~0014, 총 15 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:migrations`: 0000~0014, 총 15 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts`: D18 order tab API 3 tests 통과
- `npm run test`: admin 17 files/125 tests, customer 3 files/13 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts`: D18 order tab 390/768/1440 3 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- preview.spec.ts`: shell/sidebar 회귀 4 tests 통과
- `npm run test:e2e --workspace admin-menu-manager`: admin 60 tests 통과
- `npm run build`: 두 앱 production build 통과. admin Vite chunk size 경고는 non-failing build warning
- 로컬 서버: `http://127.0.0.1:5173/` 실행 유지, full fixture `POST /__dev/reset-auth?fixtures=full` 적용 가능

반응형 증거:

- Orders 390: `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-compact-chromium/orders-compact.png`
- Orders 768: `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-medium-chromium/orders-medium.png`
- Orders 1440: `admin-menu-manager/test-results/orders-D18-order-tab-create-detail-and-resize-at-wide-chromium/orders-wide.png`

resize 상태 보존:

- D18 E2E에서 `/bars/{barId}/orders/{orderTabId}` 상세 URL이 1440→390 resize 후 유지됨을 확인
- 상세 손님 표시/메모 draft, 헤더 바 selector 현재 선택, status filter/search state가 resize 후 유지됨을 확인
- compact·medium·wide 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인
- staff full fixture에서 sidebar에 주문 탭은 표시되고 메뉴, 카테고리, 바 회원, 사용자 계정 메뉴는 표시되지 않음을 확인

## 2026-06-23 — D19 메뉴 주문 라인·수량·void

- `0015_d19_order_tab_items.sql` migration 추가: `order_tab_items`와 `idempotency_keys` table, active/voided consistency check, line total 정수 check, tab/status와 idempotency 조회 index 추가
- 주문 이벤트 contract에 `menu_item_added`, `item_quantity_updated`, `item_voided` 추가
- 주문 상세 DTO에 active/voided 라인과 menu picker를 포함하고, menu picker는 현재 바의 visible/available 메뉴와 가격만 반환함
- `/api/bars/:barId/order-tabs/:tabId/items` POST 추가. 메뉴 라인 생성 시 메뉴 public id, 이름, price id, price label, 용량, 단가, 통화, line total을 snapshot으로 저장함
- 주문 라인 추가는 idempotency key를 요구하며, 같은 key와 같은 payload 재시도는 저장된 응답을 반환하고 같은 key의 다른 payload는 409 `IDEMPOTENCY_KEY_REUSED`로 차단함
- `/api/bars/:barId/order-tabs/:tabId/items/:itemId` PATCH 추가. 수량 변경은 주문 탭 `expectedVersion`과 라인 `itemExpectedVersion`을 함께 검사하고 서버 정수 연산으로 합계를 재계산함
- `/api/bars/:barId/order-tabs/:tabId/items/:itemId/void` POST 추가. 라인은 삭제하지 않고 `voided` 상태, reason, actor, event를 남기며 active 합계에서 제외함
- `closed`/`cancelled` 주문 탭의 라인 추가·수량 변경·void mutation은 409로 차단함
- `/bars/{barId}/orders/{orderTabId}` UI에 메뉴 검색, 메뉴/가격 선택, 수량 입력, checkout requested 재오픈 확인, 주문 라인 카드, +/- 수량 stepper, void reason 확인 panel을 추가함
- full fixture에 기존 주문 라인 seed를 추가해 화면 진입 직후 `A1`과 `Bar 3` 상세에서 주문 라인을 볼 수 있게 함

검증:

- `npm run verify:migrations`: 0000~0015, 총 16 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts`: D18~D19 order tab API 5 tests 통과
- `npm run test`: admin 17 files/127 tests, customer 3 files/13 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts`: D19 주문 라인 390/768/1440 3 tests 통과
- `npm run test:e2e`: admin 60 tests, customer 4 tests 통과
- `npm run build`: 두 앱 production build 통과. admin Vite chunk size 경고는 non-failing build warning
- 로컬 서버: `http://127.0.0.1:5173/`에서 화면 확인 가능

반응형 증거:

- Orders 390: `admin-menu-manager/test-results/orders-D19-order-line-add--82f64--void-and-resize-at-compact-chromium/orders-compact.png`
- Orders 768: `admin-menu-manager/test-results/orders-D19-order-line-add-quantity-void-and-resize-at-medium-chromium/orders-medium.png`
- Orders 1440: `admin-menu-manager/test-results/orders-D19-order-line-add-quantity-void-and-resize-at-wide-chromium/orders-wide.png`

resize 상태 보존:

- D19 E2E에서 `/bars/{barId}/orders/{orderTabId}` URL이 viewport resize 후 유지됨을 확인
- 신규 주문 탭 상세 form draft와 헤더 현재 작업 바 선택이 1440→390 resize 후 유지됨을 확인
- 메뉴 선택, 가격 선택, 수량 입력 후 같은 route에서 라인 추가·수량 변경·void 흐름이 compact·medium·wide 모두 통과함
- compact·medium·wide 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인

## 2026-06-23 — D20 기타 주문 항목·금액 조정

- `0016_d20_order_custom_adjustments.sql` migration 추가: 기존 적용 migration을 수정하지 않고 `order_tab_events`, `order_tab_items`, `idempotency_keys` check constraint를 재작성해 custom/adjustment 라인과 새 idempotency operation을 허용
- 주문 이벤트 contract에 `custom_item_added`, `adjustment_added` 추가
- 주문 상세 DTO에 D20 권한 `canAddCustomOrderItem`, `canApplyOrderAdjustment`를 포함해 UI action 노출을 권한 기반으로 제어함
- `/api/bars/:barId/order-tabs/:tabId/items/custom` POST 추가. custom 항목은 항목명, 단가, 수량, 사유를 요구하고 active 합계에 양수로 반영함
- `/api/bars/:barId/order-tabs/:tabId/items/adjustments` POST 추가. adjustment는 조정명, 0이 아닌 signed 금액, 사유를 요구하고 음수는 할인, 양수는 추가금으로 합계에 반영함
- custom은 `can_add_custom_order_item`, adjustment는 `can_apply_order_adjustment`를 별도 검사하며 staff 기본 권한은 403으로 차단함
- 같은 idempotency key와 같은 payload 재시도는 저장된 응답을 반환하고, 같은 key의 다른 payload는 409 `IDEMPOTENCY_KEY_REUSED`로 차단함
- 할인 또는 void 결과가 주문 총액을 음수로 만들면 422 `ORDER_TOTAL_NEGATIVE`를 반환함
- `/bars/{barId}/orders/{orderTabId}` UI에 기타 항목 form, 할인/추가금 form, 예상 추가/최종 금액 preview, 사유 입력, staff action 숨김을 추가함
- full fixture에서 manager 확인용 `Bar 3` 탭에 하우스 하이볼, 커버차지, 단골 할인 라인을 seed함

검증:

- `npm run verify:migrations`: 0000~0016, 총 17 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts`: D18~D20 order tab API 7 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx tests/integration/orderTabsApi.test.ts`: 52 tests 통과
- `npm run test`: admin 17 files/129 tests, customer 3 files/13 tests 통과
- `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts`: D20 주문 상세 6 tests 통과
- `npm run test:e2e`: admin 63 tests, customer 4 tests 통과
- `npm run build`: 두 앱 production build 통과. admin Vite chunk size 경고는 non-failing build warning

반응형 증거:

- Orders 390: `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-compact-chromium/orders-compact.png`
- Orders 768: `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-medium-chromium/orders-medium.png`
- Orders 1440: `admin-menu-manager/test-results/orders-D20-custom-item-adjustment-void-and-resize-at-wide-chromium/orders-wide.png`

resize 상태 보존:

- D20 E2E에서 `/bars/{barId}/orders/{orderTabId}` URL과 신규 주문 탭 상세 form draft가 resize 후 유지됨을 확인
- manager 계정으로 메뉴 라인 추가, 수량 변경, custom 커버차지 추가, 할인 adjustment 추가, 추가금 adjustment 추가, 할인 void 후 합계 복원을 compact·medium·wide 모두 확인
- staff 계정에서 같은 주문 상세 URL에 접근해도 기타 항목·금액 조정 heading/form/action이 compact·medium·wide 모두 렌더링되지 않음을 확인
- compact·medium·wide 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인

## 2026-06-23 — D21 계산 요청·정산·취소·일별 요약

- `0017_d21_order_settlement.sql` migration 추가: `order_tabs`에 최종 정산 합계·정산 시각/actor·취소 사유/actor snapshot 컬럼을 추가하고, `daily_order_summaries` table과 unique/index를 추가함
- 주문 이벤트 contract에 `checkout_requested`, `tab_reopened`, `tab_settled`, `tab_cancelled`를 추가하고 settle idempotency operation `order_settle`을 허용함
- `/api/bars/:barId/order-tabs/:tabId/checkout-request`, `/reopen`, `/settle`, `/cancel` POST 추가
- 정산은 active 라인 합계를 서버에서 다시 계산해 `finalTotalAmountMinor`에 고정하고, closed/cancelled 탭의 이후 mutation은 409로 차단함
- 같은 settle idempotency key와 같은 payload 재시도는 저장된 응답을 반환하고, 다른 payload/key 재사용 또는 이미 닫힌 탭 재정산은 409로 차단함
- 취소는 empty 또는 all-void 탭만 허용하고 active 라인이 있으면 409 `ORDER_TAB_CANCEL_NOT_EMPTY`를 반환함
- 일별 요약은 `businessDate` 기준 settled/cancelled count, settled total, settled item count를 rebuild/upsert함
- `/bars/{barId}/orders` UI에 오늘 정산 요약과 계산 요청 큐를 추가하고, `/bars/{barId}/orders/{orderTabId}` UI에 계산 요청, 재오픈, 계좌이체 확인 정산, 취소 panel을 추가함
- compact sticky action bar에 선택 탭 총액, 계산 요청, 이체 확인, 정산 완료 action을 추가하고 같은 form/query/detail state를 공유함
- full fixture에 정산 가능한 `B2`, 정산 완료 `C4`, 취소 `D1`, open 조정 샘플 `Bar 3`을 seed함

검증:

- `npm run lint`: 통과
- `npm run typecheck`: admin, customer workspace 통과
- `npm run test --workspace admin-menu-manager -- tests/integration/orderTabsApi.test.ts`: D18~D21 order tab API 9 tests 통과
- `npm run test --workspace admin-menu-manager -- tests/e2e/responsiveShell.test.tsx`: 45 tests 통과
- `npm run test`: admin 17 files/131 tests, customer 3 files/13 tests 통과
- `npm run verify:migrations`: 0000~0017, 총 18 migrations를 빈 SQLite DB에 적용 성공
- `npm run verify:no-device-routes`: 기기별 route, viewport redirect, 전체 페이지 mobile/desktop fork 없음
- `npm run build`: 두 앱 production build 통과. admin Vite chunk size 경고는 non-failing build warning
- `npm run test:e2e --workspace admin-menu-manager -- orders.spec.ts`: D21 주문 상세/정산 6 tests 통과
- `npm run test:e2e`: admin 63 tests, customer 4 tests 통과

반응형 증거:

- Orders 390: `admin-menu-manager/test-results/orders-D21-checkout-settle-a0afc-mmary-and-resize-at-compact-chromium/orders-compact.png`
- Orders 768: `admin-menu-manager/test-results/orders-D21-checkout-settle-30176-ummary-and-resize-at-medium-chromium/orders-medium.png`
- Orders 1440: `admin-menu-manager/test-results/orders-D21-checkout-settle-3f3d1--summary-and-resize-at-wide-chromium/orders-wide.png`

resize 상태 보존:

- D21 E2E에서 `/bars/{barId}/orders/{orderTabId}` URL이 viewport resize 후 유지됨을 확인
- 상세 테이블/손님 설명 draft, 헤더 현재 작업 바 selector, 정산 메모, 계좌이체 확인 checkbox가 resize 후 유지됨을 확인
- manager 계정으로 메뉴 라인 추가, 수량 변경, custom/adjustment 추가, void 후 합계 복원, 계산 요청, 정산 완료, 최종 합계 고정을 compact·medium·wide 모두 확인
- staff 계정에서 기타 항목·금액 조정 action이 compact·medium·wide 모두 렌더링되지 않음을 회귀 확인
- compact·medium·wide 모두 불필요한 가로 스크롤 없음, 44px 미만 touch target 없음 확인
